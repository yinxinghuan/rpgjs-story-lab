# 推车状态候选 · 2026-09-15

首次生成斜向等轴视角且有阴影，拒绝。一次编辑后改成轴向俯视，阴影移除；木板仍有前后宽度差，暂为开发静态候选，不认定B视角正式准入。原图均保留。

prepare-oldstreet-trolley.ts 使用项目已有浅中性色连通去底。第一次去底保留了把手围成的封闭白块，实际检查后在已观察到的空洞坐标(256,160)添加明确matteSeed，再处理后把手内部透明、金属轮廓保留。源图bbox[146,56,363,536]，输出512×640、脚点256/580，不缩放、不生成新轮廓。仅处理本张源图，SHA校验防止参数误用于别图。

实际RPG-JS物件事件采用固定graphic、stand opacity1 / hidden opacity0。身体32×28仍来自空间布局，透明帧不承担碰撞规则；碰撞从权威trolley-borrowed事实投射。事件排序跟人物处于同一引擎图层，落点在实体底边，显示比例0.13。地面另有停放位轮廓，借出后仍可选中归还；不再显示推车文字底板。提交新head先更新空间事实，再restore，防止重建地图事件使用旧事实，恢复后投射明确姿态。

借出叙述移除了尚未制作的“用完放回”牌子，只描述推车离开停放处。原历史正文不改写。

类型检查、开发构建、23项空间/Session测试通过。真实浏览器已观察停放→借出：推车图形消失、库存新增推车、归还行动出现。后续恢复与归还证据追加；当前external-guest开发视口，不是正式移动平台验收。

真实恢复闭环补验：借出后刷新仍见空停放位且背包有推车；点击归还后同一物件图形重新出现、背包移除推车、借用行动恢复。截图确认了hidden初始恢复→stand再次显示。没有通过删节点或另存本地状态补偿；实际图形与Session事实一致。角色在物件前方可遮住车板前缘，尚未完成四周绕行的完整遮挡验收。
