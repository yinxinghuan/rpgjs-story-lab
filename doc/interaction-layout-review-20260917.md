# 系统交互结构本地验证（2026-09-17）

## 实现范围

以 `interaction-layout-contract.md` 为当前结构准则。评估对象包括探索 HUD、物件/人物/门、自由输入、特写、地图、随身记录、旅程、加载与恢复、生成等待、片尾。共享规则先落在屏幕区域、主动进入、明确对象、关闭返回和长内容滚动；不是宣称所有视觉细节已完成。

## 实际操作

专用本地合成旅程服务器 `127.0.0.1:55673`，未读取玩家存档，无新模型/媒体生成请求。本轮显影台读取此前真实生成且已存下的照片。

- 暗房恢复、点击显影台并走近：无互动面板；右下目标改成显影台，主动 Examine 后才显示状态/操作。
- 320×568：开关面板前后世界元素都是320×568；没有面板挤压地图。打开调焦特写后 Close 回到无面板探索。未重复完成同一谜题。
- 暗房→照相馆→街道→修表铺：点击门只走近，右下 Enter 才转场。
- Nora：点击人物先走近；右下 Talk 完成首次介绍。短对白→话题选项；关闭重开没有残留上次对白；改点工作台先关闭对话并走近。
- 工作日志：右下按钮用 Enter 键打开实际材料特写，关闭回探索。
- 抽屉：点击、走近和 Examine 均未移箱；选择 Move box 才出现开抽屉结果，后续 Take lens 留在同一物件上下文。
- 320×568：地图、Items/Notes、旅程列表可打开/关闭；英文地图地名由挤断单词改为适当短名并保留完整可访问名称。长记录内部滚动，标题与关闭独立于记录区。
- 390×844：人物话题、材料、动作结果可用；1280×800：探索区和工具位置检查。
- 390×390：用短窗口模拟键盘压缩，仅显示输入/发送，并保留右下返回探索。实际iPhone软键盘与视口机制未验证。

截图在 `_qa/ui/interaction-layout-20260917/`。platform-layout截图通过外部访客栏自身关闭按钮收起，生产代码未隐藏扩展。

## 检查与边界

19项定向检查覆盖上下文行动、相机、短对白、显影和片尾；完整构建包含空间资源校验和Worker启动。未重复每条路线。无发布。初次加载/断线/后台等待的结构做了源码评审，未在本轮人为断网；片尾沿用此前320/390实景证据，不能算本轮全流程复验。新玩家理解仍为 comprehension unverified。

下一步仍属第一阶段：以这套信息结构评估开场引导、过程进展和等待回访的连贯性，再决定第一阶段是否收口；之后才恢复完整动态中段和技能更新。
