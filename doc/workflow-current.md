# 当前工作流交付与验收状态 · 2026-09-10

这是当前状态入口。带日期的旧交付、模型审批与云端准备文档保留为历史证据；不能据其早期“未授权/未部署”句子覆盖这里的当前状态。UI 精修后置，沿用用户已确认的 B 视角与 A 信息结构、B 临时提示、设备特写少量 C 材质。

## 已交付能力

| 目标 | 当前证据 | 边界 |
|---|---|---|
| 完整车厢短篇 | 三场景、两条完成路线、完成后继续探索；74 项测试；真实 renderer 走通电台路线 | 8–12 分钟时长仍为假设，无新玩家理解研究 |
| 视角、移动与碰撞 | 锁定 B 图集；按实际路程切换步态；30/60/120 与不规则帧逻辑测试，用户此前 iPhone 复测反馈改进 | 尺寸预览和帧率模拟不是 iPhone 性能测量 |
| 物件遮挡 | 24db97e 的固定图形事件、脚底排序、透明状态帧、整数坐标补偿；390/320 实际检查 | 暗色小电池可读性仍有限，背景墙体未拆成任意遮罩 |
| 独立工作流复用 | 从新目录完成“雨夜信号房”，新主题/双场景/状态图集/真实 RPG-JS/存档/部署 | 不把复制整款车厢当成复用；独立章为浏览器存档 |
| 受素材约束的模型扩展 | 信号房真实模型从当前设备作者观察目录选择 observationId，规则绑定场景和目标 | 有限观察选择，不是无限自由剧情；原车厢使用作者规则 |
| 生产存档与请求恢复 | 原车厢同 UUID Durable Object SQLite，owner 隔离、事务、幂等、版本/位置校验、pending 恢复 | 临时 capability 已获用户授权；无真实账号验证/跨设备账号找回 |
| 备份恢复 | 71 测试阶段新增完整备份与空库恢复；真实云端92请求含两路线备份，恢复后回执不重复发奖 | 离线空库恢复已验证，线上整库 PITR 未演练 |
| 发布 | 原车厢运行代码1e7239c、信号房9d057a1；主站/Pages实际资源 hash 核对，原车厢发布后UI复验仍有保险丝×1 | Pages 不创建第二个云端世界 |
| 平台目录 | games 清单提交583e9f8、两张正式海报已上线；Telegram Mini App 中两款均已 Publish，重载清单后均显示 Update | AlterU 实际打开、关键交互与重新进入后物品保留均通过，见 [platform-review.md](platform-review.md) |

## 当前可复用包

`deliverables/build-spatial-story-game-v1.10.zip`，30 文件；SHA256 `9ef4b27defddbd01ae4d23695ae7f87fcb4e688157610493dc21f3efcd353757`，逐文件清单为 `deliverables/workflow-v1.10-manifest.json`。

v1.10 保留已经独立消费过的 world/rpg-space/storage-scope/distance-motion 源码与检查器，新增深度排序、有限模型选择、pending/权威恢复、备份及发布证据参考。没有把游戏题材、玩家数据、凭据或完整生产后台放进包中。技能安装目录为项目 `.agents/skills/build-spatial-story-game`。

包在独立临时目录解压并运行14组检查，覆盖12帧浅色内部、失败不覆盖、非法尺寸/脚点、独立空间薄墙与缺失状态，以及移动9子项、独立空间/存储命名8子项。技能格式与所有本地 Markdown 引用检查通过。报告 `_qa/workflow-v1.10-package.json`。系统 Python 的 Pillow 为错误架构，首次检查在导入阶段失败；使用已配置的兼容运行时复验通过，未更改系统 Python。

## 验收结论与边界

当前最终 renderer 的桌面 Chromium 性能采样见 [performance-review.md](performance-review.md)，390×844、320×568、1280×720 的移动间隔与长任务已记录，异常保留。解锁后通过Chrome原生标签操作记录到真实blur/隐藏：后台距离0，返回后30秒未补跑，新输入可走到检修柜并进入特写；测试视口已恢复。IAB和访达操作未产生事件的样本不算通过，手机Safari系统挂起仍无真机实测。用户已教会 Mini App 迁移入口并授权今后自主操作，两款游戏迁移均已完成；AlterU 平台内两款游戏的关键交互、重新进入后物品恢复和信号房一次受约束模型观察均已验证，见 [platform-review.md](platform-review.md)。当前范围内的游戏、生产部署与工作流实证已完成。

线上 PITR、正式账号找回与无限生成世界均未验证或未实现，不能在交付中宣称具备。若纳入后续产品范围，应另有对应实现和实证。游戏与工作流已有上述交付；首次迁移入库与平台玩家入口打开结果均已有实际记录。已入库游戏在原部署地址更新后由平台自动读取新版，不需要重复迁移。
