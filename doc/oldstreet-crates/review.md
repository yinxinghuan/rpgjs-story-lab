# 院子旧箱候选（2026-09-15）

平台public edit任务 mt_7b34aebc7accd3093b11f25597d1cc9c，512×512，使用既有照片架公开图作为风格参考。原图、请求和任务校验均保留。生成两只封闭木箱，木色与深色像素轮廓可用于候选；箱体有转角，仍需同场检查，不宣称整套B视角正式准入。

实际背景为较暗紫色而非提示词的纯洋红。共享去底首次拒绝CLIPPED_FRAME；专用脚本只删除边界连通的紫色区域，再调用共享分帧脚点处理，不改动棕色箱体和原图。最终单帧512×512、脚点256/448。深底检查有细小紫边风险，生产准入仍为false。

像素候选入口使用old-street-crates.ts，缩放0.095，事件脚点16/28。事件初始位置和清理后位置都来自oldStreetProjectedProps；头状态变化时更新同一事件位置。原32×28碰撞体、动作条件与旧存档不变。候选下载纳入启动计数，组件销毁回收blob。

类型检查、24项Session/居民空间检查与cloud前端构建通过。真实UI复验进行中；不把构建或规则验证当作视觉验收。

真实CUA完成5456独立新旅程：街口→院子→洗衣店借推车→院子移开旧箱→地下储物室→院子→刷新。清路前截图可见两只木箱，台阶关闭；清理后两只箱子出现在墙边、入口开放，实际可通过台阶。返回与刷新截图仍见墙边箱子，推车保留。365×677视口下没有明显大块紫底，箱子未压过人物；细边与完整场景风格仍属候选检查。原完成旅程保留于旅程列表，未操作5455用户存档。空间6项检查补跑通过。
