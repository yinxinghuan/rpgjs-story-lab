# 完整委托集中试玩更新

用户要求本次发布后暂停开发，等待实际试玩反馈，再进入技能制作。本次继续现有UUID、主站、Pages静态镜像和存档后台；不另发新游戏，不改卡片名称或海报。

## 本版范围

- 探索时不常驻物件操作卡；靠近后由右下行动按钮主动进入。对话、随身、地图、旅程与物件专注界面采用已验证的内容和返回层级。
- 抽屉／柜子实物预览及可选近看；保留钟表、照片、显影专用特写。片尾使用物件与实际照片的分段演出。
- 新建旅程默认完整委托：取信→寄存记录→档案调查→屋顶搭板取底片→暗房生成及调焦曝光→选择原件去向→回家。旧旅程保留原门槛与结果，不自动升级。
- 平台生成调查命名、房间安排和照片；程序保证可解证据与权威进度，拍摄时机传给后续照片描述。必要材料提前准备，等待或失败可恢复同一任务。
- 调查发现、归还／带走／展示、人物所知及结局读取同一持久状态。继续保留完整地图、碰撞、摇杆和点击行走。

## 发布与验收

发布前的本地普通入口完整真实旅程见`campaign-live-journey-20260917.md`；最近新增调查结构的真实模型和定向机制证据见`photo-timing-inquiry-review-20260917.md`。本轮对累计变更执行一次发布回归，不逐条重新手动走所有路线。

本地1110项回归全部通过；完整cloud/Worker构建、资源hash、UUID、API base、存储隔离和公开凭据扫描通过。首轮在沙箱内有44项因本机端口EPERM失败，授权loopback后全通过；另修正一处旧测试漏列屋顶搭板机制实际使用的wood素材依赖，产品素材加载未删减。

提交本文件时尚未部署；部署后在同目录JSON记录精确commit、主站与Pages实际产物哈希、Worker健康以及线上代表性操作。没有这些结果前不得宣称发布完成。

这是集中试玩更新，不是完整单人总目标验收。真实iPhone、最终节奏与审美留给用户本次综合评价；匿名浏览器凭据仍不等同账号跨设备恢复。Pages只提供静态镜像和主站入口，不建立第二套后台。线上既有存档继续保留。

## 发布后核验

提交`dd75ee528cf028a14f95939ba5d4947d8677176a`已发布到原UUID主站，167份静态文件通过原KV发布；原Durable Object数据库绑定不变。Pages任务`35219565953`成功，包含CI回归、完整构建和旧故事空间检查。两站分别55项JS/CSS、29张旧街素材、入口、海报、许可证和存储脚本（共110项）实际内容hash全部匹配本地构建，版本标记一致。主站页面／健康／公开源码ZIP检查通过，精确结果在同名JSON。

正式URL的桌面浏览器实际续接原修表铺旅程；点抽屉寻路→右下查看→近看→返回操作正常。菜单新建探索显示完整委托，刷新后仍在街口，新旧旅程同时在列表中。没有读取访问凭据、没有提交模型输入或新媒体请求，没有重复全路线。截图位于`_qa/ui/full-trail-release-20260917/`；区分external-guest与关闭访客栏后的platform-layout。本轮不是Telegram Mini App或iPhone全程验收。

主站：<https://game.aiwaves.tech/cb90357b-fe01-48ab-b14b-0620eb0d556e/>。Pages：<https://yinxinghuan.github.io/rpgjs-story-lab/>，仅静态镜像。平台已入库且URL未变，无需再次Update迁移。

按用户要求，此次发布后暂停，等待实际试玩反馈；不继续扩展、不启动技能制作。整体目标仍未宣称完成。
