# 借阅日志的实地追查（2026-09-17）

## 改变

新准备内容可让工作日志留在档案架，或借放到照相馆放大台／洗衣店柜台。借阅条提供具体去向，原架纸层消失，对应桌面出现已准入纸夹图；读条不算取得日志证据。玩家也可先在借放位置发现日志，不强迫走固定顺序。两份证据齐全后仍回原整理桌完成原调查。旧旅程无 ledgerSite，规则、位置及完成状态保持。

这增加真实地图追查，尚非完整新章节。路线仍来自三个已支持位置，不能称为模型创造任意地图或新任务类型。

## 验证

- 完整构建通过（类型、空间准入、资源哈希、Worker 启动）。
- 28 项定向测试通过：archive、investigation-draft、campaign-story、campaign-review、attempt。包含两种外放位置到达、自由输入和按钮共享权威、借条不授予证据、回执恢复、完成主线、旧流程、密集夹页与地点合同。
- 实际 renderer：新建合成旅程；前置动作由 `_qa/commission-playtest-progress.ts` 经正常 authority 准备到档案架。随后 UI 读借阅条→门到地下室→台阶到院子→洗衣店柜台查阅→原路返回整理桌→按可见线索核对→刷新再打开，结论保持。没有隐藏状态注入或以脚本准备冒充全程试玩。
- 英文阅读窗检查实际 CSS 390×844 和320×568；浏览器既有130%缩放使 viewport 尺寸与CSS尺寸不同，已用 DOM 记录的 innerWidth/innerHeight 校正。早期截图实际300×649，已按300命名，不作为390证据。截图在 `_qa/ui/archive-loan-20260917/`。测试后恢复临时视口和语言模拟。
- 没有新图片生成、真实玩家存档或线上发布。摄影师场景分支有权威与可达性检查，未做第二次整条 UI 路线复验；核心交互与洗衣店分支共享。

## 真实生成结果

`campaign-live-20260917/loan-routes-1.json` 保存两份真实链、全部请求及一次长度修正，共7次现有 game-chat 请求。围栏修复、暴雨后清排水沟的四事件因果可用，均通过程序和模型复核；两次都返回 ledgerSite=archive 和 denseSource=ledger。**外放路线尚未获得自然模型样本，不把支持能力当成已经证明每局探索不同。**

## 下一步

内容生成需要独立的探索安排，避免模型总选同一调查方式。应在准备前安排可用的调查路径／操作类型，再让模型在真实空间能力内填充连贯内容，并对两份完整旅程进行节奏检查；不能靠反复抽模型或再叠排序题凑长度。UI真机／声音／平台综合验收、完整45–60分钟内容与旧RPG技能更新仍未完成。生产开关保持关闭。
