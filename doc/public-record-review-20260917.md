# 调查后果：公开摘要（2026-09-17）

## 实现与范围

玩家可以在调查后回修表铺，把经过写入公共记录册，也可在离开前撤下。保留私人发现，原件去向、信件、既有关系均不变；不是强制任务或长度门槛。旧旅程无调查时不新增该动作。本轮不调用模型或媒体、不改线上开关、不发布。

这一项让动态发现进入街区物件、公开知识和结局，但不是动态房间扩展或完整游戏时长的证明。上一批真实内容的因果缺陷仍未解决；不能据此宣称动态主线通过。

## 核心验证

- 24 项定向测试：原档案权威链增加未完成不能公开、自由输入写入、回执重放、场景往返及重新读取、直接动作撤下、私人发现/库存/关系不变、撤下仍能结束、重新公开后的结局。册页测试覆盖新层与旧钟/旧照的八种组合。包含已有对话规则回归；没有新真实模型调用。
- 已有隔离测试旅程从档案间经地下室、院落返回修表铺，使用真实地图行走和界面按钮写入/撤下；未注入完成事实。内容来自上一批真实模型的已保存可接受样本。
- 390 宽写入后展示结论、公开状态与撤下按钮；地图出现纸夹。最初图层落点偏低，被主角挡住，已修正并复拍。
- 320×568 撤下界面可操作，折叠匹配记录，私人发现仍可读。刷新恢复后仍在修表铺，摘要仍撤下、纸层消失。外部访客栏通过自己的关闭按钮收起后再保存平台布局证据，未改生产 CSS。
- 证据：`_qa/ui/public-record-20260917/platform-layout-published-390.png`、`platform-layout-map-published-390.png`、`platform-layout-withdrawn-320.png`、`platform-layout-map-withdrawn-restored-320.png`。

界面语言为本机中文、材料为英文合成旅程内容。视口检查不是实体 iPhone、真实听感或 AlterU 网络验收。下一步仍应推进不同的空间探索任务及与主线的因果联系，不能继续只增加记录册周边文字来凑流程。
