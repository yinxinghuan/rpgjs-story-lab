# 动态扩展计划核心实验 · 2026-09-16

新增compileExpansionPlan：固定小房间、入口、工作台、基础材质引用；内容与geometry分离。expansionPresentation允许零新图片时观察，photograph准备好后才开放拼图，atmosphere只有safeToSwap才替换。它目前是待接入的编译/展示决策模块，尚未接真实地图、持久计划任务或生图，不宣称可玩闭环。

一项针对新机制的合成测试通过：模型内容编译、缺图时基本动作可用、图像谜题局部门槛、安全时机切换、坐标不变；类型检查通过。不重跑旧故事路线。

通过现有game-chat接口做了两次合成意图测试，无真实玩家存档。第一次结构解析通过，但语义不合格：添加红窗帘/放大机，图片未到时先描述图上细节。原样保留first-plan-not-admitted.json，不准入。由此缩小生成合同：基础arrival/observation改为模板固定文字；模型仅返回title/discovery/photograph。

第二次见plan.json，基础描述已固定，模型的新照片描述可作为候选。但discovery仍写“通过放大”而模板是拼合，还推断年代生活节奏；因此本轮未准入剧情或调用生图。达到两次测试上限，停止重抽。下一步把完成动作措辞固定，并限制发现为图像支持的内容，再接持久计划与实际地图；不靠无限提示词重试。

## 新链实际游戏检查

5460独立本地服务使用新合成旅程与独立.data目录，启动模型预算2、已用1，只允许一次新增请求。实际浏览器365×677从街口到照相馆，输入“我想看看照相馆后面封着的暗房，找找老街影像。”，计划保存后出现准备入口按钮；点击后暗房门显露，通过实际门进入并查看工作台。没有注入事实、没有读取5455用户旅程，模型完成候选不直接展示发现文本。

基础地面与工作台可见、人物可走近，观察正常返回。实际检查发现白色占位按钮以及显影盘文字与复用图不一致，已修复为透明物件热点和工作台照片描述。修复中曾误将工作台加入RPG角色渲染列表，触发默认阿岚图形；已将SVG物件热点与RPG精灵列表分开，最终同场景复拍只有主角和工作台。重载仍恢复暗房与原位置，不再次请求模型。

这证明玩家意向→真实模型候选→保存→基础空间开放→实际观察的链路。新图片生成/自动准入、照片谜题开放、背景安全替换、动态发现正文仍未完成，不能宣称动态扩展完整交付。本轮无需重跑旧支线。


## 真实照片任务（2026-09-16）

本地5460实际暗房旅程通过游戏按钮调用平台媒体服务，四次独立生成均保留任务元数据和原图于photo/，不是合成PNG。第1张中央黑线，第2张仍有细分隔线；去掉模型描述中的two-half puzzle等玩法指令后第3张连续，但用户指出画风错误，实际偏摄影颗粒。第4张用明确像素簇、阶梯轮廓、灰绿/赭黄调色板替换摄影风格，实际图恢复像素描边与游戏插画感。第4张已在同一旅程内显示；它仍是候选，不代表图像拼合与叙事发现已准入。

用户明确测试不用以生成量为硬上限，因此取消此照片测试的两次硬限制。主动重做归档旧PNG；网络恢复沿用requestId/taskId，不等于重做。前3张没有用后处理掩盖问题，第四张也是平台新生成。剩余工作是候选接入可操作拼图、完成事实及发现内容，不继续无目的重生成。

用户认可第4张画风，并要求黑白。保留这张原始彩色PNG，游戏照片元素以grayscale(1)显示；不重生成已认可的构图和像素细节。后续生成提示改为black/charcoal/neutral gray/white。此项是显示转换，原图保持不变。
