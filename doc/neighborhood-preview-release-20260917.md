# 旧街集中试玩更新 · 2026-09-17

本轮在同一游戏 UUID 和同一旅程后台更新，不新建游戏。正式完成标记仍为 false；这是累计人物、环境与恢复修复的集中试玩版本。

## 发布内容

- 新旅程的玛拉 / Mara 与诺拉 / Nora 使用已接受的平台素材和有限活动路线。旧旅程保留原人物外观、名字与经历。
- 街口与合住院环境补图、门口和屋顶轮廓表现；堵台阶的箱子贴合实际通行边界，清障后视觉与碰撞一起改变。
- 记录册显示已经同意记录的照片和旧钟；动态照片下载失败可重新加载原图片，未结束生成沿原任务恢复，避免重开请求。
- 场景 PNG 已成功载入且具有有效尺寸时，不再因为 decode 回调未结束永久等待。具体故障复现与实际恢复证据见 `oldstreet-image-ready-20260917.md`。
- 更新旧列车封面为旧街宣传封面；目录名称拟同步为 The Last Letter · Preview，沿用原 UUID、正式地址及海报文件名。

## 本地发布前证据

- 合并测试：1005 项通过。旧的 turn 测试硬编码许青，与默认新旅程的诺拉冲突；改为分别验证中英、新旧人物旅程，未修改产品角色逻辑来迁就旧断言。
- 构建通过，9 个原作场景 / 27 份资源的 hash 检查通过，Worker 启动检查通过。构建仍提示部分 bundle 大于 500 kB，不代表初次进入性能已经验收。
- 实际 localhost 游戏中声音开 → 关 → 刷新 → 仍为关 → 开，原旅程恢复正常。脚步按位移、停止/转场不产生脚步、材质切换三项音频规则测试通过；未以这些证据宣称 iPhone 音质或音画同步听感通过。
- 平台生成封面 1024 原图与 160 缩略图已查看；它是宣传插画，透视和人物比例不作为场景/角色生成基准。来源见 `poster-provenance.json`。

## 尚未完成与后续顺序

提交时线上部署和双站实际资源校验尚待执行，成功后另记精确 commit、资源 hash 和运行证据。平台目录更新与平台实际显示区分记录。

### 发布后核验补充

`7e29e895d242db7e8034bd41d24311d39c297448` 已部署到原 UUID 主站和 Pages，后者 Actions `35143037890` 成功。实际请求两站各 7 项（HTML、含本轮标记的入口 bundle、海报、许可证、院落、新店主与新摄影师图集），14 项内容 hash 与本地 dist 完全一致；主站页面 / health / Remix ZIP 检查通过。精确记录见 `neighborhood-preview-release-20260917.json`。首次资源检查发生在 Pages 尚在构建时，它返回旧 HTML；待该运行成功后再检查通过，未误报双部署提前完成。

Telegram AlterU 内重新打开目标游戏后，恢复了既有“信已送到”结局与放大镜、小格钥匙。新建合成测试旅程，街口门进入照相馆成功，新摄影师诺拉显示并完成招呼，旅程列表同时保留新探索与已完成旧旅程。这里只验证本次更新的进入 / 角色 / 转场 / 旧档保留，未重复全故事，也不是 iPhone、账号跨设备或新模型验收。

目录仓库 `9858cfb` 已发布新版名称、说明与同名海报，平台卡片实际显示新海报，但平台数据库标题仍是旧列车名称。平台 Edit 进入创作 Agent 而非名称表单，因此没有向它追加修改指令。随后 Mac 锁屏，元数据同步尚未完成；下次可操作平台时通过既有元数据维护入口完成，不为此再发游戏代码。

本地 390×844、320×568 及本次平台窄屏发现的交互问题已记录到 `ui-polish-review-20260917.md`：行动重复、对话选项被挤出可见区、选择对象和就近按钮不一致，以及入口构图留空。下一轮先修这些实景问题。

本轮不补跑已耗尽单项额度的真实模型测试，不把匿名续玩宣称为可信账号跨设备恢复。iPhone 实际听感、完整 AlterU 流程、自由输入失败用例仍保持原未完成状态。

用户最新要求：最终制作技能之前，完成一轮集中的实际交互与 UI 打磨。按 requirements.md 的行动、交谈、地图探索、等待恢复和手机适配要求推进；本次上线不等于 UI 已完成，不继续频繁发布小修。
