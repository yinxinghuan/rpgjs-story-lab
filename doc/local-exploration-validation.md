# 本地优先的玩法验证

## 目标与发布节奏

优先验证玩家通过地图自由探索推进故事的完整结构，减少依赖用户远程解锁及保持平台窗口前台。本轮线上试玩保留为阶段参照；其后通常本地迭代，阶段交付或必须在线验证才能继续时再发布。

## 日常入口

使用 Node 22.22.2。项目根目录执行 `npm run dev:playtest`，打开 `http://127.0.0.1:5455/`。默认新探索试玩；旅程菜单中折叠的旧版参考通向 `?story=original`。本机两种权威服务维持独立存储，沿用旧存档，不为了测试清空存储。

执行 `npm run test:exploration` 可无前台窗口验证旧街规则、地图连接、物件状态、角色介绍、解谜证据、旅程回执和恢复。执行 `npm test` 扩展到旧版、创作和公共能力回归；部分用例需要允许临时回环 HTTP 监听。测试均使用专门测试数据库。

## 验证边界

| 层级 | 优先方法 | 不代表什么 |
| --- | --- | --- |
| 地图、碰撞、解谜、路线、结局与人物状态 | 本地确定性测试；新增玩法同步增补有意义的行为断言 | 不证明真实画面舒服、出口容易理解 |
| 断线、重复请求、刷新与丢失响应 | 本地 Worker/HTTP 故障注入及隔离测试旅程 | 不证明真实广域网延迟和平台桥接 |
| 人物步态、镜头、触控、对白布局 | 本地真实 renderer 试玩和截图 | 不证明 iPhone 实机或 AlterU 宿主行为 |
| API 部署、资源缓存、公开接口版本 | 必要时直接请求线上接口和实际 bundle | 不需要一概要求解锁 Telegram |
| 平台登录桥接、真实 WebView、iPhone 与平台内输入 | 集中到必要节点做真实应用测试 | 需要可操作窗口时如实记录，不能冒充已通过 |

## 下一轮本地重点

1. 让可见门口与地图连接直观一致，验证往返、绕行和带道具返回后的变化。
2. 完善检查、拿取、使用、归还、交谈的就近行动与短对白，避免测试术语进入产品界面。
3. 在保持已选 B 视角的前提下补齐空间美术与角色活动，逐项验证脚点、遮挡、碰撞、状态图一致性。
4. 扩充能够由玩家发现和选择推进的内容，避免恢复话剧式强制逐幕转场。

当前候选美术未全部正式准入；规则通过不等于玩家理解或美术验收通过。局部开发故障不应让所有工作等待用户解锁。

## 本轮证据（2026-09-15）

- 发布源码 `2b855e2eb02e412da3ae5f2c2aa3a99813502e5c`。主站默认入口可载入旧街，点击修表铺门后实际移动并转场；经「旅程 → 旧版参考」进入列车，再从设置返回新版，恢复到修表铺。
- 主站健康接口明确报告 preview=true、production=false；入口 bundle、旧街 bundle 和抽屉大图与本地构建 SHA256 一致。
- 本地探索回归 57 项通过。完整回归最初发现历史 vendor 清单漏记两项空间扩展；保留上游哈希并补记原有修改后对应 16 项复验通过，随后 GitHub 全量测试通过。
- 本次没有进行 iPhone 实机、完整 AlterU 内试玩或弱网验收，不将入口与接口验证写成平台验收。

- Pages 正常 main 发布 run `34927596731` 构建与部署成功，提交与主站一致。两端实际入口 `index-BbkR4U10.js`、旧街模块 `old-street-dev-C6VTVgXE.js` 均包含 `oldstreet-preview-20260915`，主站资源与本地 SHA256 核验通过。开发分支 run `34927059208` 的测试/构建成功，但被 Pages 环境分支保护拒绝部署；未修改保护，改走正常 main 快进发布。

## 独立本地主线试玩（2026-09-15，5456）

- 保留用户 5455 的旅程，另开回环端口 5456 创建独立测试旅程；不清空用户存档，不发布线上版本。
- 通过实际 renderer、地图热点、地面行走和行动按钮完成：街口 → 照相馆认识小许并询问路线 → 屋顶 → 工作棚认识老周并借钥匙 → 拉开院门门闩 → 合住院 → 修表铺开小格、拿信 → 原路工作棚归还钥匙 → 街口回家。
- 全程未使用自由输入、未注入完成状态。开锁后小格依次显示有信、空；归还后随身只剩密封信；结束后显示「信已送到」且随身为空。刷新仍恢复同一结局。
- 实际发现并修复：主动走开后旧对白和人物选项残留。现在点击地面开始移动会收起两者；手动移动会收起尚在展示的对白及其选项。自动走近并执行的行动继续保留所选目标，不影响请求执行。这里只清除瞬时 UI，存档中的叙事块不被删除。
- 在工作棚用真实地面点击复验：人物行动选项消失，随身物品保留，接着能经院门返回并完成主线。
- 本轮验证只覆盖该主线路径及上述 UI 修复；并不代表其他支线、手机实机、平台宿主、场景美术或新玩家理解已验收。六个场景仍有白盒地面、人物活动和内容深度待完善。
- 自动探索回归 63 项通过，TypeScript 检查通过。正式云模式构建在本地验证，不部署。
