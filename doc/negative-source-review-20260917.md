# 档案线索 → 实地底片 → 冲印：核心机制检查

新建开发委托 letter-trail-v4 先从档案得到底片位置，经照相馆楼梯、屋顶缺口、搭板、开柜拿取，再在放大台准备冲印。来源可自己携带或寄存在照相馆，旧旅程不改变流程。没有新增场景素材、模型／媒体请求或生产发布。

## 已验证

- 20项定向检查通过（archive、campaign-story、roof-supply）。两条新来源分支均覆盖未取禁止冲印、正常物理路线、唯一库存、刷新与重复提交、放大台距离、后续照片选择和结局；既有委托与旧档语义保持。
- 本地正常 renderer 从已完成合成档案出发，实际完成：读取底片线索、经门返回照相馆、放大台显示缺少来源、上屋顶搭板、跨越缺口、开柜取底片、刷新恢复、随身回看唯一底片、回放大台提交、实际地图出现暗房门。
- 测试夹具 `_qa/negative-source-playtest-server.ts` 使用内存数据库，前置由正常权威行动准备；生成任务回放合成历史和既有PNG，本轮没有新调用外部叙事或媒体服务。
- 完整构建通过，9场景／27资源SHA与Worker启动检查通过；公开凭据扫描通过。

## 边界

本次是核心流程验证，不是全部路线、真实 iPhone 触控或生产网络验收，不证明45–60分钟内容量。档案仍使用现有整理机制。部分浏览器截图及点击坐标出现缩放不一致，后半流程用键盘操作；不能称手机点击全部通过。courtyard南出口与控制区接近的问题已列入系统界面待复现项。

证据目录 `_qa/ui/negative-source-20260917/`。390初始档案截图有效；后续标称320截图存在缩放差异，仅用于状态参考，不能用于像素尺寸验收。
