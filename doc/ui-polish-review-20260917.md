# 交互与 UI 首轮实景审查

检查版本：`7e29e89` 本地实际游戏，保留既有合成测试旅程。此次查看街口、照相馆入口及首次招呼，未运行新的模型请求。范围是界面发现，不是完整验收或新的风格定稿。

## 实际看到的问题

1. **行动入口重复**（390×844，街口）：底部“当前行动”卡片包含合住院按钮，右下角同时出现同名按钮；卡片还常驻“已恢复旅程”与“随身：无”。静止探索时占用地图却没有增加决策信息。优先合并到一个明确主行动，恢复提示短暂出现，空背包不常驻复述。
2. **对话选项被正文挤出首屏**（390×844 / 320×568，照相馆）：人物介绍与第一句对白同时出现，下一步话题在 390 下只露出部分，在 320 下完全位于可见面板之外。DOM 中存在按钮不代表玩家自然发现。需要短对白节拍、稳定选项区与可辨认的继续/离开，不以更小字体压入长文。
3. **选中对象与就近按钮不一致**（照相馆门内）：选中远处人物后，卡片显示“打个招呼”，右下角仍为“街口”。两者规则有区别但没有清楚表达。应统一玩家当前意图与行动目标，允许取消走近，不能让两个入口暗示不同下一步。
4. **入口构图出现大片场景外区域**（390×844，照相馆入口）：人物与房间集中在屏幕上半部，下方深色区域较大。需核对房间边界、相机跟随与面板预留空间的共同作用，在保留满屏放大感的前提下改善，不直接缩小人物以塞完整张图。

## 下一轮实现与验证

先处理上述行动/对话的结构问题，再统一字体、图标、面板与动效。不重新选 A/B/C，不扩展调试功能，不改玩家已有事实。每次只复验受影响状态：街口选择出口、人物招呼与后续话题、退出交谈恢复走动、窄屏输入键盘、安全区和加载恢复。英文长选项尚未检查，需要纳入。

当前只有实景问题观察；没有声称界面已修好、玩家理解已验证或手机实际触控已验收。完整技能制作继续置于修整之后。

## 本地实施与复验

- 已将主行动统一为显式选择优先，探索时撤去重复行动/空背包常驻行；预设话题和独立的自由输入展开入口保留。实际照相馆点击楼梯话题得到许青的短对白；结束交谈后面板收起、右下角恢复交谈按钮。
- 320×568与390×844浏览器检查短对白、可选话题和自由输入。输入展开时可滚动，未模拟iPhone软键盘，不能声称真机键盘已通过。旧旅程的许青身份没有被改成诺拉。
- 使用现有已完成合成旅程播放7段尾声；无需重新跑整条故事。实际确认照片归还、老周钥匙未还、诺拉后续等内容来自这份结局。暂停/上一段/继续/跳过/重看/查看旅程可操作，可重新选择未完成旅程。
- 首次实景发现HUD的22px SVG规则影响片尾人物，并有邻帧露边；已独立覆盖艺术图尺寸、添加单帧clipPath，随后检查主角和照片镜头。按钮采用尾声专属规则，避免继承探索页白底和footer间距。
- reduce模式手动逐段显示、取消图片倾斜和自动播放；320×568实景通过。图片加载失败仍保留文字回顾。截图在当前工具对话中，未写成仓库内图片证据。
- 本地测试19项通过，最终增量完整构建通过，9个原场景/27个资源SHA校验与Worker启动检查通过。对改动源文件执行UI静态扫描无图标/emoji命中；全目录扫描会命中历史dist，不据此宣称全项目零命中。

仍待：英文真实长文布局、iPhone键盘与触控、相机边界留白、声音收束和完整代表性体验链；本轮不是最终UI验收，不制作交接包、不追加小版本线上发布。

## 英文、短对白与镜头边界复验（同日增量）

- 英文 320×568 的长工具标签曾挤压房间名，改为同一套 SVG 图标与 Map / Items / Journeys 短标签，完整可访问名称保留。实际检查 Street、Photo studio 与 Riverside workshop。
- 短台词与紧随的动作旁白合并为一个显示节拍，保留各自说话身份和原文；长介绍仍分页，避免递钥匙的一句旁白也要求额外点一次继续。
- 320×340 的缩短视口模拟曾将 Send 挤出面板，展开输入时暂收正文/选项，保留关闭、输入及发送。已输入后清空，未发送模型请求；该模拟不等于 iPhone 真实软键盘验收。
- 本地全新英文合成旅程经真实页面完成：见诺拉、见老周并借钥匙、院门/修表铺取信、带信离开。320×568 检查逐段尾声，390×844 检查结尾与旅程列表；刷新仍能播放这份已保存结局，钥匙未还与只认识诺拉的事实未被夸大为支线完成。此路线也证实当前主线可绕过全部物件归还支线，第二阶段需增加实质主线内容。
- 空信格现在描述信已取走的实际状态，不再只提示通用“已处理”。
- 尾声双音的解锁、静音、有限时长和停止旧音效通过合成 AudioContext 测试；尚无真实手机听感证据，不能称音质已验收。
- 照相馆入口 390×844 实景证实旧相机露出约三百像素的场景外区域。修正为按完整视口约束地图边界，底部阅读带只影响取景目标；保证纵向铺底并保持世界坐标/碰撞/点击换算。320×568 再看照相馆、通过南门到街口：边界无场景外长条，转场与脚点正常。外部访客栏通过它自己的关闭按钮收起，生产代码未增加隐藏逻辑或避让。
- 改动相关的 18 项测试通过（含镜头、动作、对白、尾声、音效）；上轮完整构建与秘密扫描通过，镜头最后修改另行完成最终构建。画面证据为本任务工具截图，未保存为仓库图片。

本地 UI 基线的主要问题已修复；真实 iPhone 键盘/触控、听感和发布后的网络体验仍留在整合验收。没有据此宣称整体目标完成，没有发布新线上版本。

## 对象说明与走近取消复验

- 修表铺实际点击抽屉曾只显示“眼前”和自由输入入口。标题现在使用同一物件状态名称；已取走放大镜时直接显示收据仍在，避免空泛面板。320×568与390×844实际检查，收起面板前后的地图矩形完全相同（本次320视口：x=-13.7143,y=-90.2857,w=438.852,h=658.281），没有推动世界。
- 使用浏览器开发拦截暂缓一次本地移盒行动，在到达前/后分别观察接近与执行状态；恢复请求后抽屉打开，再取放大镜，操作连续。拦截已经清除，没有持续网络修改。随后将接近时按钮改为可点击“停下”，执行阶段仍显示“正在行动”。
- 渲染器原先收到方向键/失焦会取消路径及到达回调，界面却不知道自动走近已结束。新增可选 `onRouteCancelled` 回调，仅在取消带到达行动的路径时通知。旧街清理对应忙碌/待回应状态；成功到达先移除取消处理，再进入权威行动。
- 实际洗衣店：开始借推车并按方向键，恢复可操作、没有借走；再次从远处接近，按名称点击“停下”，提示已停下且仍可借推车。一次较慢的人工点击已赶上到达后的归还按钮，该次只作为借还操作，不计作取消证据。
- 输入“我想借用这辆推车”后开始接近，立即停下，输入原样恢复且操作可继续。测试文字随后清空。没有调用模型。
- 320宽洗衣店查看话题/自由输入控件；临时切换界面语言后，英文 Leave conversation、Say something else、Talk/Act 均可见。当前存档为中文，姓名和旧话题保持中文，此项只证明英文控件布局，不冒充英文生成内容测试。语言覆盖与视口均已恢复。

结合前面的英文完整旅程、解谜/归还/结局及恢复证据，第一阶段达到本地可用交互基线。接下来进入第二阶段内容与动态探索；真实iPhone/声音/平台网络继续留在整合验收，不能据此宣布正式游戏或总目标完成。

本轮28项相关回归测试、最终生产构建、9场景27资源SHA、Worker启动、修改视图静态UI检查与秘密扫描通过。路线取消本身的证据来自上述实际浏览器操作，不将其他移动单测冒充取消集成测试。
