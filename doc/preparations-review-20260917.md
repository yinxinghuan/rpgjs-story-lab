# 开场、准备等待与回访验证（2026-09-17）

## 实现
- 开场短操作说明、开始探索、旅程菜单回看帮助。
- 随身可查看既有任务的等待/就绪/失败/断线和返回地点。后台跨场景观察只查询，首次恢复已有候选不通知，不替玩家读材料或转场。
- 放大台读过外借日志后进入共用上下文，避免日志独占后续内容入口；相关前置条件按当前用途显示。

## 证据
真实地图使用专用合成旅程数据库，无新媒体/模型调用。在修表铺的随身面板看到暗房已有实际照片的就绪任务；320×568与390×844截图位于 `_qa/ui/preparations-20260917/`。既有委托已读外借日志的放大台可先显示普通上下文，再主动重新阅读日志；原有暗房已开放，本轮不重复生成新的暗房。

新建基础探索旅程（没有改动正式新主线开关）验证开场文字、按钮与320/390布局，按开始探索后面板消失；旅程菜单可再次展开 Controls。旧的完整委托保留并已恢复。此为机制和布局证据，不能称新玩家理解通过。

本地组件夹具 `_qa/preparations-preview.html` 使用真实观察Hook与显示组件、合成状态接口：准备中→查询断线→完成后恢复→切换合成旅程，观察到就绪通知一次，新旅程不带旧提示；记录7次读取、0次写入。这里只验证状态衔接，不能冒充真实平台断网测试或真实生成延迟。夹具不进入Vite正式入口。

21项定向测试覆盖只读查询、无任务、断线与失败区别、就绪去重/旅程隔离、未揭示/已准入任务、扩展恢复及现有日志连续性。完整构建另含空间素材校验和Worker启动。

## 剩余
系统UI第一阶段继续核对目标与进展在完整动态委托中的连贯性；需要将本次共用物件规则用于完整新主线实际路径验证，不能仅据组件测试宣布整阶段完成。iPhone真实键盘、平台网络与完整动态内容质量仍保留后续验收。未发布线上。
