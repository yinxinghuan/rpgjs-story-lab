# 完整委托的干净本地入口

新启动命令 `npm run dev:campaign`，访问 http://127.0.0.1:55677/ 。这是原主游戏的开发配置，不是新验证游戏、UUID或生产发版。使用持久本地数据库；没有直接注入进度，也没有回放假内容。

## 本次完成与证据

- 首次进入直接显示“取信、查清旧事、相关旧照”委托；正常菜单只有另开探索，没有调试主线按钮。
- 从菜单另开一段仍是完整委托，刷新后随身页保留同一目标。旧旅程由原目录继续，新的默认选项不会迁移旧档。
- 8项入口／本地恢复／拒绝文案检查通过；测试包含旧短档不改、另开新完整档、开户丢回执、明确拒绝后读取断线且不重复提交。
- 正式构建通过（9场景、27资源SHA、Worker启动）。浏览器证据在 `_qa/ui/campaign-entry-20260917/`；本次仅检查启动、另开与恢复，不重走已验证剧情。

两个新拒绝码此前未加入客户端终结判定，可能造成永久pending；现已修复。测试中的拒绝响应由合成transport提供，后端条件判断已有上一轮来源链检查，不把此次合成错误注入称为真实网络故障。

## 未完成

当前已连接的动态中段仍主要是档案调查与相关照片，不因开放本地入口就等同内容深度、45–60分钟时长或正式交付。仍需完整体验节奏、真实平台服务长链及最终手机／生产验收。底部出口遮挡待定点复现。当前开场检查没有发送模型或媒体请求，后续实际试玩到生成节点会走原平台服务。
