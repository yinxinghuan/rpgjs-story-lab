# 下一阶段顺序（2026-09-17 用户确认）

最新第二阶段增量：生成布局已接入真实家具、碰撞和互动；两次真实请求中一份不可达拒绝，一份已在390/320宽实际进入、调查、往返并刷新恢复。33项定向检查通过，详见 `archive-room-review-20260917.md`。这是房间摆法变化，不是新任务类型或完整动态章节验收；下一步推进不同调查行为与主线后果，并验证组合生成的语义和节奏。未发布线上。

顺序：当前 UI/交互收口 → 当前 2D 精灵式 RPG 的完整主线与动态中间内容 → 旧文字/电影式 RPG 制作技能正式更新。不并行扩大三个方向；继续现有游戏，不据此创建新 UUID、游戏或改写旧旅程。

当前状态：第一阶段已达到本地可用基线（证据 `ui-polish-review-20260917.md`、`material-interaction-ui-20260917.md`），下一步集中第二阶段。真机触控/键盘、声音听感和线上网络属于整合验收未完成项；本地基线不等于正式交付。

当前第二阶段增量：两段材料链后已接入第三段“档案工作间”调查：通过自然侧门进入，查两处记录，在桌上还原事件顺序，保存发现并参与新旅程结局条件。旧旅程不加门槛。实际浏览器发现的首次开放家具缺失、空行动面板和小屏成功结果藏于底部已修复；27项核心定向检查通过。详情见 `archive-playtest-20260917.md`。下一步先完善这些发现与主线目标的因果联系，避免为了长度强迫无关支线，再验证两份真实生成内容和整段实际节奏；不以合成谜题证明完整内容量。线上功能继续关闭，未发新版本。

## 1. UI 与交互收口

完成移动/摇杆/附近行动反馈、短对白与行动选择、自由输入、加载衔接、随身物品/旅程入口、小屏中英文适配及电影式结局演出。保留满屏放大、跟随镜头及已接受美术方向。

验收一次代表性的“探索→对话→用物品/解谜→切场景→结局→续玩”流程，复验受改动影响的核心状态，不反复走全部路线。检查画面不跳动、按钮清晰、测试功能隔离、手机尺寸不溢出；自动化/视口模拟与真实手机、声音听感证据分开记录。

完成标准：没有妨碍探索的主要交互问题，形成可持续使用的 UI 基线，不以无限视觉精修拖延第二阶段。本地优先，阶段版本合并发布。

## 2. 2D RPG：完整主线与动态中间内容

当前取信即可离开的主线和可选暗房扩展不等于完整动态章节链。先绘制主线依赖与自然地图出入口，说明玩家每段发现、选择、解决的事情及持久后果；主线目标、关键因果和结局条件固定，中间部分内容可以每局不同。章节是探索进度，不是强制舞台式转场。

先验证两个相接的动态探索环节，证明一次完成产生的真实线索/后果能约束下一个生成环节，再按体验扩到约 2–3 个主要动态环节。数量是本游戏实验规模，不是通用技能硬指标。45–60 分钟延续为内容设计目标，未实测前不声称达到；不靠走路、等待和重复对话凑时长。

生成实例需要进入条件、可用实体、行为、完成证据、后续出口和持久 ID，经过准入后才改变游戏事实。优先预备下一环节；空间布局、碰撞、出入口和必需线索齐备才可进入，非关键氛围图可后补。不让玩家在空白无规则场景行动，也不要求全部装饰先生成完。失败恢复不授予完成事实。

同一旅程保存已生成内容与规则版本，刷新不重新生成，旧旅程不追加新门槛。通用运行时能力与服务支持须据代码/API逐项验证，不把现有单一暗房模板或随机预设当成已实现任意动态章节。

验收：两份真实生成且决策/线索关系有差异的旅程；一条最短合法主线及一条代表性探索路线；一次恢复和一次慢/失败生成。分别记录核心机制、实际内容、节奏和未验证体验。模型/媒体调用沿用有效授权，新增调用范围必要时说明具体缺口。

完成标准：能体验完整主线，有真实动态中间内容，结局由足够的已完成因果决定；不能由一件早期道具或模型一句话直接触发。保留合理替代解法与明确提前退出，不把所有支线强制化。

## 3. 旧文字 / 电影式 RPG 制作技能更新

现有技能的问题分析和设计要求可先记录，但本阶段才进行完整模板/规则/验收改造及正式打包。参考 build-stateful-story-game、create-rpg-game 和 create-hosted-rpg 的实际实现，不自动迁移后台。

将第二阶段验证的主线里程碑、动态中间内容准入/恢复、章节与终局区分、最短通关路线检查移植到各自适用的壳。文字式保留开放行动和累积阅读；电影式的字幕页/图片步骤不能作为剧情进度。不是所有 RPG 都要长篇、同样章节数或强制动态生成。

对“Prolog 后太快结局”需检查受影响作品的迁移前后定义和执行轨迹，不能仅凭时间相关性认定规则引擎有错。分别补充制作要求、已有模板能力和服务缺口；不能用提示词代替权威结局条件，不能宣称技能文档更新已经修好线上作品。

验收：文字与电影式各一份代表性产物，确认章节可继续、真正结局条件准确、自由行动没有退化为猜预设口令；动态能力只在实际支持且验证后标为可用。最后更新技能包与验证记录；已分发包和线上旧游戏的升级范围另列，不自动批量发布。

## 推进方式

按阶段自主推进，不逐步索要审美确认。汇报当前阶段、已完成、剩余和实际阻塞。优先验证核心机制；只在缺有效授权、真实平台能力或无法替代的外部条件时请求用户协助。保持现有大目标，技能制作不能先于游戏机制的真实验证。


### 第二阶段实施记录

2026-09-17 街区后果：完成调查后可在修表铺写入/撤下公共摘要；真实纸夹图层、私人发现/原件分离、自由输入与按钮同权威、公开知识及结局读取同一状态。24项定向检查和390/320浏览器操作/刷新通过，详见 `public-record-review-20260917.md`。此项不增加结束门槛，不代表动态房间、内容质量或完整时长已通过；线上不变。

2026-09-17 收尾复验：可接受的停电/路面维修样本已在真实 renderer 完成两处查阅、整理、结论与刷新恢复，320宽结果按钮保持可用。随后最终 `otherEvent` 格式新批实际五次请求：一链结构完整但种树/计划/采购先后不合理，另一链片段超长拒绝；没有完整内容合格链。停止继续打磨单一排序题，下一项先补中段调查的实际探索目的及路径/物件后果，再验证生成内容的变化。线上开关仍关闭。详见 `campaign-live-20260917/inquiry-review.md`。

2026-09-17 两事件调查：问题与固定事件身份绑定，档案来源和结论共享同一关系链。新六请求得到两组三段结构，第一组因主题漂移/事件卡泄题拒绝，第二组停电与人行道维修的调查作为机制样本接受。随后将记录主题改为程序固定、增加显式泄题拒绝；最终提示格式尚需新批次实测，不能宣称两份动态旅程均通过。详情见 `campaign-live-20260917/inquiry-review.md`。后续在可靠执行基础上推进真正的探索差异与完整内容量，不继续反复刷同类排序样本。

2026-09-17 真实生成首测：六次请求暴露寄存记录无匹配项，已将配对结构改为程序组合、模型只生成词项；修复后的两份记录都可解。一组三段结构通过，但档案缺少问题所需的维修完成事件，内容评审拒绝；另一组只完成记录生成。下一步优先解决问题—证据—答案的真实因果及调查方式差异，不宣称动态中段已经验收。报告 `campaign-live-20260917/review.md` 保留全部原始输出和失败。

2026-09-17 因果衔接：新 v2 旅程从开场明确取信及查清记录的委托；材料片段提出问题，档案生成继承它，结局回应调查结果。旧旅程不补写动机。中英文新旧存档、知识可见范围、三阶段权威流程的定向检查和正式构建通过；实际手机宽度的开场及纸袋→档案问题衔接见 `campaign-commission-review-20260917.md`。这修复了强制调查缺乏动机的问题，但没有证明故事已达到目标时长；下一步仍是两份真实生成内容的因果、变化及体验节奏，不继续靠附加同类排序题增加长度。

2026-09-17：可选新旅程的两段内容实例、权威比对/取舍、结局条件、内容保存与回执恢复已接入现有后台并通过30项合成/旧链路测试。玩家入口暂未开启；下一步完成地图中的材料互动界面、自由输入衔接及后台准备，再进行实际模型生成与浏览器流程。未开启或迁移任何线上旅程。

2026-09-17 UI收口补充：旅程菜单固定关闭按钮；材料候选视图完成320/390宽的滚动、选项和断线恢复检查。后台草稿准备与新旅程配置恢复已有合成验证，候选只在本地调试入口开放。真实生成和地图全链路尚未验收，自由输入衔接仍待第二阶段；不据此宣称动态主线完成。详见 `material-interaction-ui-20260917.md`。

2026-09-17 第二阶段增量：自由输入已接入当前旅程的动态材料动作与已观察知识，查阅/比对/取舍共用原权威规则；发现页可回看材料，未完成时不会提前提示回家。30项定向合成测试通过，320材料发现页修复英文页签拥挤并固定关闭入口。尚缺真实生成/理解、主地图候选完整试玩、正式Worker任务接口和完整内容量，详见 `campaign-input-review-20260917.md`。
