# 图片编辑链路对照 · 2026-09-17

- 已知两条新造型 edit 请求返回 PROVIDER_REJECTED，无图像；旧日志只保存错误码，不能追认其具体原因。
- 增加 public MediaServiceError 的 HTTP 状态/消息记录后，一条最小“同人物只换背景”请求（9a8f8b62-e74a-433e-b284-7c273cf53308）返回502，错误明确为读取 raw.githubusercontent.com 超时。该请求实际传入jsDelivr公开参考；客户端确实发送该URL，不能擅自断言后端为何读取raw域名。
- 本机两条公开参考均HTTP200且为PNG；本机可读不证明生成后端可读。
- 将参考改为此前平台生成、平台CDN托管的原图，最小背景替换请求成功，task mt_92fc330f3c91be15f33fc5c269391060，约11.1秒。目视背景绿色、人物/大致布局保持；此图本来就含错方向姿态，成功不等于合格动作。
- 因参考内容也变化，这不是严格同图多域名对照，也不是GPT模型对比。能支持的是：当前至少一条edit失败属于网络读取错误，使用现有平台CDN参考可以完成edit；不能把PROVIDER_REJECTED统一解释为提示词或人物问题。
- 后续人物生产优先采用已验证平台CDN参考，不再盲目重复GitHub来源失败；长期仍需服务端准确区分网络超时与不可重试生成拒绝。未更改公共客户端重试规则，也未自动重试终态任务。
