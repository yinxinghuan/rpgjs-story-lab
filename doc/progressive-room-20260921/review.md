# 渐进暗房试点：2026-09-21

用户选择：自动检查后显示，保留回退。不是逐件等人工签收，也不是开放任意生成地图。

生成服务两项任务均成功，原图、平台完整 task 与 SHA256 见同目录 floor/bench PNG 和 JSON。地板为完整方形材质；工作台为洋红底独立物件。浏览器验证尺寸、内容哈希、透明度与轮廓比例后显示，标记含义是 auto-admitted，不是美术全部合格。

共享正式组件、hook 和真实 RPG renderer 在 390×844 / 320×568 下运行：骨架、仅地板、仅工作台、完整、部分失败，每项覆盖中英，共 20 组。观察到轮廓没有洋红背景、台体没有拉伸、独立槽可逐件显示，角色可沿相同地面移动。回退控件改为折叠菜单，避免常驻大按钮挡住家具。地板当前颗粒偏明显，属于本轮自动生成结果，不借此改动用户已选择的精细门风格。

证据：`_qa/ui/progressive-room-20260921/report.json` 与截图。历史展开截图为完整可滚动页。脚本还检查当前轮排除、最多四轮、浏览器真实音频开始播放与静音、离开后晚到素材不显示、回退选择刷新仍保留且停止媒体请求。

边界：API fixture 使用已完成的真实生成文件；后端持久化/幂等另由 SQLite 和 Worker 测试覆盖。尚未串行完成“正式平台普通旅程进入暗房→现场生成→重进”的端到端试验；未做实体手机、最终 HUD 集成美术验收。回退 fixture 验证选择与请求停止，不作为原有工作台素材的外观验收。音频尚待完整验听及循环接缝评审。本次未发布。

最终本地检查：Node 22.22.2 全量 1142/1142 测试通过；`npm run build`（前端、空间检查、产物 SHA256 检查、Worker startup/health）通过；API base 审计、能力档案校验、空间 RPG 技能校验、`git diff --check` 通过。最初沙箱运行有本机监听权限错误，授权回环网络后复跑；新 capability 字段的两个旧断言已更新。构建仍提示历史 bundle 体积较大，不把它报告成零警告。
