# AlterU 内旧街主线试玩（2026-09-16）

运行位置：Telegram 的 AlterU Mini App，现有游戏 UUID `cb90357b-fe01-48ab-b14b-0620eb0d556e`。线上为已核验的 `73deaaf` 试玩版本；后续本地修正未发布。未提取、复用或保存窗口树中附带的认证信息，未打开真实玩家存档；本报告不保存平台 URL 查询或原始窗口树。

## 实际操作与结果

- 系统锁屏解除后恢复访问。旧列车测试页面提示版本不一致，点击“重新载入游戏并恢复”后进入新版旧街初始街口；原列车旅程未重置或删除。
- 从街口进入修表铺，移开空盒、拿放大镜。抽屉标签从被挡住到放大镜再到收据，行囊同步加入放大镜。
- 经街口、照相馆、屋顶到河边工作棚。未介绍人物仍以外形称呼，借钥匙后显示老周介绍、具名短对白与递钥匙旁白；行囊加入小格钥匙。
- 棚侧院门初始关闭，抬起插销后开放。经新门返回合住院，旧箱仍挡着地下室台阶，未被无关转场改变；返回修表铺时抽屉取物状态保持。
- 用钥匙开小格、拿密封信；小格标签依次显示密封信和空，背包加入信。
- 街口选择带信回家，确认页明确提示钥匙尚未归还。选择离开后显示“信已送到”，密封信从背包移除，结局记录钥匙仍持有。实际平台截图中没有外部 guest-shell 访客栏，结局与操作按钮可见。
- 点击查看旅程。首次列表读取被自动审批拒绝；用户随后明确允许读取，才继续读取列表。列表返回本次街口“当前旅程”的服务器记录；未打开或修改其他旅程。

## 证据边界

这是平台内实际主线从空行囊到结局的操作证据，覆盖自然转场、取物、人物首次介绍、开门、借物后果和完成记录查询。所有行动使用固定按钮，没有调用新增自由输入或媒体生成；此前待答复的单条模型补测授权不因此扩大。

尝试打开 Telegram 顶部菜单以重新载入，自动化返回 `AXError.notImplemented`，截图确认菜单未展开；未继续盲点、退出账号或重建旅程。因此本轮不证明重新启动 Mini App 后的结局恢复，也不证明 iPhone 硬件上的摇杆、步频、声音、真实断网恢复、动态暗房全链路或后台可信账号跨设备恢复。工具自身单次调用耗时不作为游戏网络延迟指标。

平台测试旅程保留在已完成状态。本地主线、自由尝试故障注入与本轮平台证据合并看待，不能用任何一项替代 NPC 行走素材、完整生产美术准入与其他未完成交付条件。

## 重新进入补验：未通过

用户手动从平台重新打开同一个游戏后，先显示静态加载页，随后进入旧街恢复错误画面：“暂时没能确认这一步的结果。请重新连接，我们会核对进度后继续。”点击游戏内“重新连接并恢复”后仍观察到同一错误画面，尚未读回原结局。没有点击重新探索、清理缓存、重建身份或删除旅程，因此不能断言存档丢失，也不能把此前旅程列表存在记录当作本次恢复通过。

同期从终端只读请求公开 `/api/oldstreet/health` 返回200/ok，runtimeContract为 `oldstreet-session-1.map-2`，耗时约2.9秒。它仅说明公开健康接口可达，不能定位平台WebView中的会话请求失败原因；没有读取认证参数或直接查询私人会话。恢复错误的真实根因仍待排查。静态HTML加载标题残留旧列车名，已本地改为通用“故事探索 · STORY EXPLORER”，未发布，不影响现有存档。
