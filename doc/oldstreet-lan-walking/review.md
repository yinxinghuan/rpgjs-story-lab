# 阿岚行走候选：拒绝作为动作素材

平台 public edit 任务 mt_cff23da5863b18bd4c8e20787c0218b9，960×1408，沿用原游戏 UUID 与已公开阿岚参考。request.json、state.json 与 candidate.png 保留真实请求、任务和输出校验。没有私有账号或玩家数据输入。

可见问题：第二排第一帧面向右，与另外两帧向左冲突；正面中间帧双脚前后错开，不是待机；背向两端步态差异不足，未能确认互换领先腿。不能靠镜像整个人物或去底把这些问题算作解决。walkingAdmitted=false，productionAdmitted=false，没有接入旧街，也没有改变现有站姿。

为验证平台素材到制作页的同一处理链，scripts/prepare-oldstreet-lan-walking.ts 使用共享 prepareSpritePixels 的 magenta 模式处理并另存 cutout.png。原图保留，12 帧以 160/328 对齐；去除 1061172 个连接背景像素，边沿校正 9291 个像素，核心保留 268590 个像素。这些数字不证明画面合格。深底检查仍能看见正面手臂附近及部分背面袖口的封闭紫色区域，需明确选点；当前未修。

新背景模式是制作流程能力，不是此人物准入。真实 PNG 已通过共享处理、草稿清单保存/恢复与地图几何解析测试，恢复后没有新增 actorReview。后续若继续制作，应先用单方向少帧验证方向和交替步态，不能把整张十二帧请求持续重试当作进展。
