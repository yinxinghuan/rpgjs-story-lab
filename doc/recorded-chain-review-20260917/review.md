# 连续生成留档与交互复验（2026-09-17）

本地开发分支，未发布主站/Pages。总目标仍未完成。本轮将前一轮未提交的真实链证据归档，并修复实际界面问题；不是新增独立游戏或交接包。

## 已留存的真实请求

- `../campaign-live-20260917/full-chain-after-roof.json`：两条新合成旅程的真实文字生成共 11 请求（上限 18），保留原始输出、修正及复核。主题分别为人行道铺板、社区花园围栏。
- `report.json`：将两套已接受文字以完全相同的场景上下文回放进正常 Session/jobs，再调用真实平台媒体服务。2 张实际 PNG 位于 `chain-1.png` / `chain-2.png`。首次受网络限制的失败与沿原请求 ID 恢复均保留，未另起生成意图。
- 两条权威行动链分别 56 / 47 次，包含真实地图寻路、自然入口、材料取舍、日志路线、照片决定、归还工具和结局恢复。全部由程序驱动，不等于玩家实操或时长证据。脚本为 `scripts/test-oldstreet-recorded-journeys.ts`。
- 图片主体与故事大体一致、黑白像素风可用；不能仅凭图片证明修缮发生时间或具体新旧材料。两条剧情仍类似，排序仍容易凭常识猜出。没有据此宣布动态中段多样性、深度或 45–60 分钟成立。

## 实际界面检查及修复

使用原本地 QA 数据库中的合成 `synthetic-route-trial-2`，从已准备的暗房照片节点开始。未读真实玩家档案，未新发生成请求，非从开场全实时生成的浏览器验收。

1. **P1 续玩语言混用**：浏览器中文、存档英文时，HUD/行动等仍取浏览器语言。现以存档为准，并修正初始化、切换与延迟准备场景时的提示闭包。实际 Roof terrace、Journeys、Darkroom、Photo studio、Street、显影及片尾均为英文。
2. **P2 显影状态误导**：未做调焦曝光就提示“照片显出来了”。新显影方式在素材就绪时改为“底片已备好，可以调焦与曝光”；旧拼图方式不改规则。
3. **P1 决定后说明遮挡地图**：选择照片去向后仍在显影台附近常驻发现大段。现自动收起，点选显影台仍可查看。实际确认留下→刷新→默认收起→点选→回看→关闭→从房门离开。
4. **P2 动态照片未进入片尾**：关于围栏照片的字幕曾只配主角。现在在该照片去向/发现段展示本旅程实际 PNG，hash 与已提交的显影版本核对，加载失败仍可读字幕。不修改结局、不触发生图。

## 实操结果与边界

- 390×844 开始调焦，按照片边缘与明暗提示调到清晰；320×568 确认→选择留下→刷新保持。
- 实际经暗房门→照相馆门→街口→回家。该浏览器分支选择仍带钥匙离开，界面有确认提示；这与程序链归还钥匙的选择不同，不混作同一结局证据。
- 320/390 实际片尾显示同一围栏照片和该旅程发现。图片回看、关闭、暂停、继续可用；没有做 iPhone 真机键盘/触控验收。
- 7 项针对显影与片尾的检查通过（包含新照片与固定照片夹分离、中英文与不改保存状态）。完整构建通过，9 场景/27 资源 SHA 校验和 Worker 启动检查通过；仍有既存的大 bundle 警告。改动 UI 文件静态扫描及项目秘密扫描通过。
- 截图目录 `_qa/ui/recorded-chain-20260917/`：platform-layout 为关闭外部访客栏后的游戏构图；external-guest 保留扩展。没有修改生产访客栏。

## 下一项

UI 本地基线已有代表性证据，保持第一阶段的小缺口已收口。第二阶段仍须把固定主线与不同实地调查组成有意义的动态中段；当前两套相似维修排序题只能证明接口整链，不证明完整游戏可玩深度。旧文字/电影式技能仍排第三，不先把未验证机制写为正式标准。
