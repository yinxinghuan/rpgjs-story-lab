# 平台美术生产链验证计划

2026-09-11，依据用户对内置生图工具与创作者/游玩实际能力不一致的纠正。此项前移为下一阶段首要工作，在继续批量生产原作区域、人物与设备之前完成；UI 精修和多人仍后置。

## 当前事实

- 当前 B 方向的部分场景、人物与设备候选由 Codex 内置 imagegen 生成，用于验证相机、比例和视觉方向。它不是平台用户可以调用的正式生产能力。
- 本工程已经接入 AlterU 公共媒体客户端和旅途画页任务恢复，但此前两次真实画页请求均返回 PROVIDER_REJECTED，未得到可评审结果，正式画页入口关闭。这既不能证明当前服务整体不可用，也不能证明列车 B 风格能够量产。
- 当前素材只是目标样本，尚不是经过平台媒体服务验证的生产质量承诺。图片格式为 PNG 不代表具有真实 alpha。

## 共用链路

创作者描述/玩家授权的内容请求 → 世界和视觉合同 → 媒体任务计划 → AlterU 媒体服务 → 候选验证 → 存储和版本登记 → 同场景渲染验证 → 激活。

制作期和游玩期调用同一平台媒体边界及同一候选验证规则，不依赖 Codex、GPT6 工具权限、本机脚本或人工修图。模型供应商及版本调整由平台媒体服务负责。自然语言由应用侧导演转换为受约束的英文生成提示，用户不必会写提示词。当前接口 edit 只有一个公开 HTTPS 参考，不假定它支持独立多参考/多身份输入。

## 执行顺序与通过条件

1. **真实服务先跑通一张**：用现有公开、已授权的项目参考及合成内容，确认 text/edit 可用路径，记录输入、服务任务 ID、响应、耗时和实际文件。不反复重试被明确拒绝的相同请求。失败时定位请求合同/参考/服务原因；不得以替换内置生图工具的结果冒充服务成功。
2. **三类素材质量对照**：环境背景、四向三帧角色图集、设备状态图各最多三个候选，第一批总上限九张，包含第1步样本。提示词可按平台能力调整，每次修改留痕；不要求随机生成器逐像素复现，但必须满足已认可 B 方向的角度、尺寸、人物比例、清晰度、状态一致性和可玩要求。角色检查真实透明、边缘、十二帧角色身份/四向/步态与脚底；设备检查各状态形状、尺寸及锚点；地图检查背景与模板几何/碰撞一致。每张记录人工修复次数、可用率、耗时和服务实际提供的费用；服务没有费用数据则记录未知。九张只能作为可行性样本，不代表长期生产可靠性。
3. **在同一个游戏里换入实测**：并排保留现有样本，实际替换通过的背景、角色和设备，在同一 RPG-JS 地图测试行走、碰撞、遮挡、设备变化及 320×568/390×844。只对通过的类别开放生成能力，不以一张背景成功推断动画图集也可用。
4. **创作者自助闭环**：无本机和内置工具参与，用创作者入口从主题描述生成一个最小场景、角色和可互动设备，经历等待、失败重试、候选预览、激活和发布版本。此最小实例留在现有项目，不另建验证游戏或交接包。
5. **游玩期闭环**：先开放非关键特写/旅途插画，然后测试有模板约束的物件或身份变化；个性化先用一份普通人和一份无脸/非人合成参考另作有界测试。新角色或新区域须在准备完成并通过表现检查后才进入可互动世界；等待时保留旧场景与可用行动。已激活资产写入存档版本，刷新不重画，迟到任务不能覆盖新进度，重试不重复计费或结算。不能承诺首次实时生成人物图集/地图无等待。

## 质量不足时的处理

先确定是否可通过提示、单参考、尺寸及可部署的标准处理改善。需要去背景、图集拆分或对齐时，必须成为创作者和游玩链路可调用的能力，验证自动化边界；本机人工处理只能作为研究证据，不能标记自助生产通过。若服务模型无法稳定产出相应类别，使用已准入素材库/模板组合，暂不开放该类别的全新实时生成；或由平台服务维护方评估统一升级生成能力。不得把未确认可用的 GPT6 图形 API 当作既定依赖，也不静默降低已认可的视觉基准。

通过以上服务样本、真实替换及自助闭环后，才继续扩展大量原作素材并固化面向用户的制作工作流。原有规则、存档和渲染开发成果继续使用，当前正式游戏不受候选替换影响。

## 第一批实测结果（2026-09-11）

已通过公共媒体服务发起五个有固定 request ID 的合成请求，五个均成功，约 9.7–13.7 秒；费用接口未提供，不能估算实际扣费。输入、原始 PNG、任务结果、摘要及逐项判断保存在 `platform-art-candidates/20260911/`。同请求重放与任务查询返回同一任务和素材 URL；这验证任务身份恢复，不等于独立核实账单。

| 类别 | 实际结果 | 准入结论 |
| --- | --- | --- |
| 无参考背景 | 有画面，但车体长度、地面占位及轨道透视偏离模板 | 拒绝 |
| 单参考背景变体 | 同地图检修点、车体阻挡和人物落点实测通过；偏暗、细节更平滑 | 可继续实景研究，未达到生产质量持平 |
| 新角色四向图集 | 新服装可生成，但侧面/背面未遵循，且 RGB 白底 | 拒绝 |
| 原角色换色图集 | 四方向改善，但棋盘格被画进 RGB | 拒绝 |
| 设备三状态 | 状态和大体角度可辨，但 RGB 白底、基线未按要求对齐 | 拒绝 |

真实透明检查解码了全部图片，三个角色/设备候选均没有 alpha 或 tRNS；未使用本机修图补救。背景候选仅进入同工程 preflight 对照页，正式 cloud/Pages 不包含它。桌面与 320×568 实际画面和行走已检查；390×844 DOM 尺寸/落点经缩放补偿核验，截图工具出现缩放留边，不能将该尺寸的构图评审标记完整通过。198 项自动化检查通过。

下一步优先解决**可部署的真实透明处理和角色四向/状态锚点合同**，同时完善背景亮度与细节保持；不要仅增加同类重抽次数。剩余四个候选额度是上限，不是必须消耗的次数。背景、角色、设备分别准入；创作者自助闭环和游玩期激活仍未完成。现有素材继续作为质量目标，不能用本批五张成功响应宣称平台已能稳定量产同等游戏美术。

## 浏览器制作入口实证（2026-09-11）

同工程增加 `?create_art=north-cape`，在 preflight 用浏览器直接调用公共媒体客户端，持久保存原请求/任务、字节与历史候选。第六个有界请求 `7ec82edb-2fdf-4c90-9461-99f908531729` 在 localhost 实际返回 `ORIGIN_NOT_ALLOWED`，没有服务任务 ID 和新图片；刷新仍显示同一失败，并禁用原失败状态下的无效重生成。未改变或伪造浏览器 Origin，未改用去掉 Origin 的代理绕过。原先五个成功样本结论保持。

另用先前已成功的平台背景进行真实样本载入，明确提示非本次生成；浏览器保存→刷新→同地图试走→左侧检修/车体阻挡→返回制作页→390右侧检修通过，320/390 表单和地图无横向溢出。此前390截图缩放的未决项，在本次 DPR=1、实际390×844下完成复查。206 项回归、三种构建和媒体集成检查通过。UI 静态扫描只报构建依赖的文本符号，未报告本次源码功能图标问题。

只读任务请求附带正式站点 Origin 返回200，为下一步部署验证提供线索，不能代替真实正式来源的浏览器 POST/下载/刷新测试。下一步在现有游戏受支持的部署来源验证完整创建链，继续解决透明、方向、锚点和质量差距；不把“已有样本可载入”或“任务GET成功”算成创作者自助生成闭环完成。

## 正式来源的新生成已跑通（2026-09-11）

提交 `05db41c` 在原游戏部署内提供独立 `creator.html`，主站与 Pages 两个入口及八个实际资源均与本地同提交构建逐字节一致，Pages 运行 `34521169011` 成功。后台沿用原 UUID、DO 绑定和协议；原游戏两条完整路线、支线、终章、备份恢复合计204个请求通过，206项本地回归通过。

在正式来源由浏览器直接发起新请求 `85e52bdb-7796-473b-971e-9b9f0af5afcc`，服务返回 `mt_1a1c4492493eaf68a32331d43d91c207` 和新图片。浏览器下载、解码、保存、刷新后同版本恢复，并在原地图中行走成功；不是载入旧样本，也未使用内置生图或本机修图。它是总第七个意向请求、第六张实际图片、第三张背景，服务报告 timing_ms=4405，费用仍未知。

`environment-edit-02` 的亮度、纹理和轮廓比首轮平台变体改善，接近基准；桌面及320/390下四个落点、车体阻挡与画面坐标通过。这张**固定参考、固定布局的背景变体**通过具体检查，但不由此宣称其他背景、角色或设备能稳定量产。它仍是制作草稿，没有激活新剧情场景。生成已在刷新前完成，因此本次真实请求只证明完成后的刷新恢复；中途断线恢复仍以之前任务重放和合成故障测试为证。

后续优先级：可部署透明处理、角色四向/设备锚点与身份连续性 → 创作者云端草稿与版本发布 → 游玩期内容激活和失败恢复 → 继续补齐原作单人区域/章节/结局。已有固定背景制作和预览链可复用，不重复搭建新实验游戏。

## 浏览器透明与图集处理基础（2026-09-11）

`src/sprite-preparation.ts` 提供与生图提供方无关的 RGBA 候选处理，`sprite-preparation-client.ts` / `.worker.ts` 放到独立 Web Worker。输入选择 `pale-neutral` 浅中性连通去底或 `alpha` 已有透明保持模式；原始数组不转移、不覆盖，输出独立候选。现有媒体公共合同没有透明参数或去底端点，本模块是游戏制作端代码，不是新增的媒体服务接口。

人物按声明的脚点对齐，不缩放每帧；设备必须提供每状态的柜体接地点，避免打开的门角改变柜体高度。浅中性模式仅估计边缘混色，保留无可靠内侧参考的小块浅色细节，禁止从其他帧取颜色。网格不可整除、空帧、原图截断、输出容不下或设备缺锚点时拒绝。已有半透明像素不再次去污染；原生透明模式不按颜色删白色像素。

八项合成像素测试已覆盖以上合同。实际浏览器加载 Vite 构建的 Worker，在 1280×720 桌面处理960×1280/12帧合成图约121ms，10ms界面计时器触发12次；原输入摘要不变，输出与同步实现逐像素一致，调用前取消、运行中取消、结构化克隆失败及随后重新运行通过。详见 `sprite-preparation-review-20260911.json`。这个耗时不是iPhone性能数据，15秒超时分支未做真实等待故障注入。

真实图像的非生图工具处理仍有待回应的明确授权问题；本轮没有读取或修改任何真实素材，也没有新增媒体请求。处理代码尚未接入制作页按钮、PNG编码、候选持久记录或正式剧情准入，不发布到生产。尚待真实素材评审的风险包括与背景连通的白色服装、封闭棋盘格孔洞、彩色背景和小块灰色残留；算法不识别人、脚、物件或四方向语义。不得把这些合成测试写成角色或设备已准入。下一步接候选原图/处理参数/输出摘要与可回退记录，再验证真实素材和同场景动画，方向不合格仍拒绝。

## 素材候选保存与预览已接入（2026-09-11，开发分支）

同一个 `creator.html` 的人物/设备入口现在支持PNG原图/固定平台样本、显式参数、后台处理、原图/结果比较、深浅底、候选导出链接和历史版本。源图与参数、PNG输出摘要、算法/帧偏移一起保存；新处理另存子记录，跨页当前版本检查阻止迟到覆盖，失败和刷新保留已有记录。此前“尚未接制作页、PNG编码、候选保存”的状态已由本轮代码与合成浏览器实证补上；尚未补的是实际角色/设备质量和地图试用。

220回归通过，真实浏览器合成PNG处理与保存、刷新同摘要、错误参数拒绝、旧候选恢复、中英320/390通过；正式构建能只读载入平台设备原图并按原摘要保存，未执行真实图处理。图像工具规则所需的真实图代码处理授权仍待用户回应。本轮无新增媒体调用、无素材准入/生产替换或发布。公开服务没有新增端点，本能力属于创作者浏览器代码。后续完成实际去底、四向/状态/同地图动画验证后，才决定类别是否准入，再推进云端版本发布与游玩期激活。

## 已保存人物候选的同地图试走（2026-09-11）

人物准备结果现在可通过固定候选ID进入原作同一RPG-JS检查地图，经真实透明/帧/脚点/摘要检查后替换检查页外观，并能明确回退基准。首次实际运行发现blob纹理没有被默认解析器加载，已通过显式PNG纹理parser修复并同状态重拍；不能只靠运行位置探针判断有角色。

合成四色方向图集已经走过北岬与河谷，在390/320尺寸验证方向、运动帧标记、落点、车体/水面拒绝、刷新同候选及基准回退；缺候选时停止且提供返回准备入口。参考B人物真实可见高度已只读测量，候选按整张统一尺度显示。224项回归通过，本轮无实际资产处理或媒体生成、无生产发布。真实人物边缘/动作质量和设备状态试用仍未完成，不能把合成图进入地图当作这两个类别准入。


## 固定背景进入原作旅程（2026-09-11）

环境edit-02原字节已登记为north-cape-platform-8fc11a96，用于新建原作本机Story Session；既有旅程不带绑定时继续使用north-cape-baseline-e8e36bba。原Head保存明确版本，构建/下载核对摘要和尺寸，未知绑定或错误文件拒绝、不静默替换。该背景的绑定、真实寻路/行动/刷新和390/320复验完成；人物和设备仍未准入。

本轮无生图调用或真实图片后处理，计数保持7次意向/6张实际图片。494项回归及补充8项HTTP资产贯穿检查、三种构建与媒体/秘密/API审计通过。详见background-binding-review.json。工程资源登记已推进，但尚没有创作者账户云端草稿/发布服务或运行时任意候选激活；这些边界不能由一个静态发布版本冒充完成。下一步继续可部署素材准备的真实类别验证和创作者/游玩共同的发布激活链。

## 在线背景草稿持久保存（2026-09-11）

同一制作页新增在线保存/列表/取回，沿用原平台媒体任务并按不可变版本保存原图、SHA、原请求与任务ID；现有DO绑定内隔离制作身份，最多6个候选。实际本机服务读取已有平台任务并保存2221448字节原图；重启后同SHA取回，浏览器再次解码保存后进入同一RPG-JS地图行走/碰撞。此轮没有新生图请求或图像后处理，计数仍7个意向/6张图。535项全套与5项专项通过。

这补上了服务端原图存储/断线恢复基础，未完成平台账户绑定、正式部署、发布准入或游玩期激活；浏览器身份丢失后的跨设备取回仍未提供。角色和设备真实质量门仍未通过。下一步应在同一主项目衔接发布/激活及平台身份，而非继续复制独立候选页面。


2026-09-11 创作者背景发布与游玩绑定增量：同一制作入口要求四个真实落点/车体阻挡和显式画面确认，修复寻路网格取整漏记后五项通过；发布不可变原图版本，私有草稿保持鉴权，玩家新旅程由服务解析发布元数据并锁定素材。旧登记/存档语义保持，原作wire升4/assets2。537回归及真实浏览器发布版本维修、刷新、实际河谷转场/再次刷新通过，燃料仅68→62、车况82→87，源图43块2811504字节SHA一致。该检查为创作者声明而非自动审美认证；尚未正式部署和账户绑定，真实人物设备/其余场景与完整原作平台验收继续。无新生成/模型调用、真实图处理或玩家数据读取，不将一个背景版本闭环当作完整游戏完成。


### 阿达第三个角色候选（2026-09-11）

沿用首批角色最多3个候选的上限，此次仅1次新生成意向actor-edit-03，总意向从7到8。使用公开已授权B主角图集作为姿态/相机参考，尝试保留全部姿态并改变年轻女机修学徒外观。检查全部四方向、12帧一致性、实际透明与脚底；成功响应不自动准入。已有两个角色候选分别因方向错误及RGB棋盘格拒绝，不覆盖它们或现有正式素材。


2026-09-11 用户明确授权：允许使用本项目制作页代码，对平台生成的真实人物/设备PNG自动去背景、分帧和脚点对齐；原图保持不变，另存候选，不发送到新外部服务。该授权持续有效，同范围不再重复询问。真实图处理待授权状态已解除；语义/方向/边缘准入仍须验证。


### 用户指出基准背向同腿错误（2026-09-11）

用户在实际图集中发现背向两步均同腿在前，黄色B主角基准也有该问题。原距离驱动切帧正确不能替代原图左右迈步语义验收。暂停阿达图集准入，优先修正基准；新增一个用户指定的背向步态修复候选hero-back-stride-01，单次请求，上限1，不继续扩充阿达三候选批次。该结果需检查脚部先后反转、背包侧别不跳和真实运行节奏；不自动覆盖旧素材。

2026-09-11 阿达站姿进入原作：在已有授权范围内复用制作页同一自动处理，裁出actor-edit-03正面站姿并保留源图，未使用其他错误方向。原作RPG-JS实体与当前人物存在/碰撞/交谈共享权威状态，实际河谷交谈→接应→治疗→隧道转场/刷新及缺图恢复通过，v2→v7，最后50燃料87车况64人心。542回归及三构建通过。仅正面站姿在preflight接入；整张图集、其他角色设备和人物自助发布仍未通过。320/390 DOM及操作有证据，宿主130%缩放导致部分截图留边，不能宣称完整手机视觉或正式发布完成。此轮无新媒体/模型请求，继续总体单人和生产链目标。

2026-09-11 设备第二候选：先检查props-edit-01真实去底与三个柜体锚点；该保险丝柜不能冒充原作的柴油柜或启动继电器。按首批设备最多3候选的既有上限，本轮仅新增starter-edit-02一个意向，使用已公开B设备参考，生成关盖/烧黑继电器/铜片修复三状态。总图片预计从8到9（其中另有用户指定主角步态修复1张），未成功前不计为完成；不自动覆盖原图或正式资源。

第三次设备请求只引用02的铜线圈单帧，返回starter-repair-03，保住铜线圈身份并增加铜色连接条；耗时8769ms，320×640，161124字节，费用未知。设备3次有界机会用完，不再扩大本轮重生成。累计本批9张成功图片，另有用户指示的主角步态重生成1张，共10张；含一次无图片的来源拒绝共11个意向。保留02整图拒绝结论，只将其铜线圈帧与03组成原作两状态候选。新增真实设备素材入场与原权威修复事实联动，不等于自助设备发布链完成。

2026-09-11 浏览器两来源设备组合：补齐此前启动继电器需本机装配的步骤。创作者页面支持两份PNG及各自选帧列，保存完整原图/摘要/组合信息，以同一浏览器PNG与Worker链去底对齐；两状态正确显示修复前后，旧三状态保持。真实现有平台样本经浏览器组合、处理、刷新、北岬/河谷切换与390/320检查通过，549回归通过；门角伸到脚点前方的碰撞预留同时修正。本轮没有新增生图或模型请求，没有覆盖原图或生产素材。OS文件选择路径尚未实际操作，设备云端发布与生成仍未完成；这一步补生产链缺口，完整单人目标保持。


2026-09-11 制作页在线原图入口：新增固定B修正步态参考的阿达图集配方及铜线圈启动机前/后单帧配方，沿公开媒体客户端；每次意向固定ID、任务先落盘、8秒及服务限流等待，恢复不重复提交新意向。合成响应回放旧PNG在真实制作页验证下载503/刷新/续接/导入/去底/实际本机在线归档和取回，来源随候选与组合两原图保持；设备可直接从保存记录选帧组合。564回归及11最终专项、三构建和审计通过，320/390 DOM与截图可操作。无新增真实生图，首批额度未扩大，新配方质量尚未通过；完整人物、其余场景、平台身份、受支持来源实际创作和正式单人平台验收继续。详见sprite-generation-review-20260911.json。
