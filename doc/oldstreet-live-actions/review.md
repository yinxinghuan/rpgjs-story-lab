# 旧街真实模型行动测试 · 2026-09-15

使用现有 AlterU game-chat 接口和新建的内存合成旅程；不读取真实玩家存档、账户或凭据。固定上限为 6 次上游请求，本次实际 5 次。脚本 scripts/test-oldstreet-live-actions.ts 需显式 OLDSTREET_LIVE_TRIAL=1，不进入普通自动测试。

结果见 result.json：
- “我现在借用那把开寄存信小格的钥匙。”：模型分类及复核后提交 borrow-key，钥匙入包，版本 +1。
- “归还钥匙？”：本地意图检查拒绝，不调用模型、不还钥匙，版本不变。
- “我把借来的那把钥匙交回您手里。”：模型分类及复核后提交 return-key，钥匙从背包移除，版本 +1。
- “我给自己变出一架直升飞机。”：模型返回不支持，没有新增实体或改变存档。

实际修复：旧街本地别名匹配原先先移除末尾问号，可能把疑问句当命令；现在先运行与模型相同的意图 veto，再匹配完整别名。作者明确登记的“询问是否留下钟的故事”等单个行动仍按其完整标签识别。

Worker 支持注入旧街解释器，本机预检读取显式 OLDSTREET_MODEL_TEST_BUDGET；未设置时不调用在线模型。正式发布仍关闭。Worker 路由注入、无模型按钮、疑问别名拒绝及原 Session 测试合计 20 项通过；TypeScript 和 Worker 构建通过。

边界：本次真实模型验证直接经过 OldStreetAuthority，Worker 注入链另用合成提供者验证；未声称真实模型已在 AlterU 浏览器内完成联测。四例不代表全部自然语言、开放对白、长期记忆或完整游戏内容已验收。
