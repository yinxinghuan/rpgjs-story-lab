# 抽屉三态素材评审 · 2026-09-15

目标：同一个物件的关闭/打开有放大镜/取走放大镜后三态；沿用 B 俯视方向，和店铺地图尺度及碰撞统一。平台 text 生成一次，再 edit 定向修正一次，均已返回 succeeded；这只证明服务调用成功。

首版失败：桌面近边比远边明显宽，为梯形透视；左帧抽屉已经打开，盒子闭合，和作者规则“移开空盒后拉开抽屉”不符。右两帧的物品差别大致可辨认，但不能替代整套状态语义。

修正版失败：盒子已经变成可见中空的开口盒，但左帧抽屉仍然敞开；桌面仍是同样梯形。不能通过改变玩家规则文案掩盖素材不符，也不把整体压扁当作透视修正。

两份原图、精确请求、平台 task/request ID 和 SHA 均保留在本目录；不进入 public、运行图集或正式准入。未执行去背景，因为语义/透视已失败，先抠图并不能修复这些问题。首版只读检测 alpha 恒为255，背景实际以 (237,18,219) 一带为主，并非提示词指定的纯 (255,0,255)，因此后续去底不能依赖精确等色删除。

下一次资产制作需使用同坐标的三态轮廓参考：桌面矩形/桌脚锚点/抽屉开闭与独立盒子位置由素材布局确定，模型只补纹理。每一帧的可见结构通过后再去底、校对尺寸并放入真实 renderer。本轮不继续第三次随机生成，不更改旧人物视角或正式发布。
