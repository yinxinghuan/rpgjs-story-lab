# 设备候选实际地图检查

现有主游戏的制作页增加设备三状态预览；本轮使用合成色块检查图，不作为正式美术质量通过证据。没有新游戏、媒体请求或模型请求，没有正式部署。

- 浏览器通过可见制作页载入 240×128 合成图，设为三状态，目标脚点 40,110，原图与候选分别保留。候选 ID `afb69bb0-4c7b-412a-9318-3d07317d565f`。
- 三状态实际像素可辨：关闭青色柜门、打开褐色门扇和橙色物品、取空后保留门扇但移除物品，柜体位置和大小不跳变。
- 北岬柜前 104,236，柜后 104,204；河谷柜前 276,424，柜后 276,388。画面位置与运行位置一致；后方人物被柜体遮挡，前方人物覆盖柜体。开柜/空柜均不能穿过固定占地。
- 切换河谷保留有物，切为空柜后返回北岬实际保留空柜；刷新重载同一候选并恢复预览初始关柜。候选预览不是存档里的已拾取事实。
- 初次柜后点击被寻路取整拒绝：增加 4 单位余量后同地图、同候选实际复验。原失败截图保留为 `device-back-grid-rejected-before-fix-platform-layout-390.png`。
- 390×844、320×568 浏览器视口可操作，44px 状态/检查按钮，无横向溢出；这是桌面浏览器尺寸验证，不是真 iPhone 性能结论。

证据在 `_qa/ui/device-*.png`；包含外部访客栏桌面检查及关闭访客栏后的平台构图检查。只做设备接入的功能/构图判断，合成色块不作风格评分。实际平台设备语义、边缘质量、创作者云端发布及故事资产绑定继续验证。

不存在的 `missing-device-qa` 在实际浏览器中显示明确错误，移动/状态按钮禁用；返回准备页仍能找到原图与原候选，再进入原 ID。原图摘要 `22aa319763cdde29e9274f36f39e2766c62a9d299fe480d5da779de5aa8c437b`，候选摘要 `cd2f9e1a6c27b9b7aa993268e2645589a3ec741fd5709122994f9a620076c471`，算法 `neutral-matte-unmix-1`。

验证：518 项全套回归通过，网格边界修正后 4 项设备针对性测试复验通过；preflight、正式前端/Worker、Pages 构建通过。秘密/API base/媒体合同审计通过。上游缺音频 URL 和大 bundle 警告仍存在，不在本轮声称修复。
