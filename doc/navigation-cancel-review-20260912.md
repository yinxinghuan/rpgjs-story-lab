# 自动行走与面板切换复验

当前修改：打开记录、行囊、人物、附近或结局时同步暂停 renderer，取消尚未抵达的路线，避免下一帧的抵达回调覆盖玩家选择的面板。抵达回调还校验旅程 ID、场景 ID 和状态版本。

已有本地浏览器证据：`_qa/navigation-cancel-ui/platform-layout-390-journal.png` 与 `platform-layout-320-people.png`。记录、行囊、人物取消场景已经操作；但关闭人物面板后重新点击物件，曾观察到目的地消失而行动面板未打开，原因尚未确定，不能宣称完整衔接通过。

补充机械回归：`_qa/original-world.test.ts` 使用正式 WALK_SPEED，分别在 30、60、120 fps 的行走途中每 23 帧采样，从各中断位置重新规划到北岬全部规划交互点，逐步执行碰撞检测并验证到达。4 项测试通过。该检查排查规划几何和中途恢复路径，不证明 DOM 点击、实际素材占地或面板回调正确。

2026-09-12 再次恢复浏览器时，旧测试 tab 109 不存在；随后 CUA 返回电脑已锁定且自动解锁失败。真实 UI 复验待屏幕恢复。未清除任何存档，也未发布这次修改。
