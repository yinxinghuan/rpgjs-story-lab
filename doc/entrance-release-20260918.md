# 通道与实体门更新 · 2026-09-18

本次保持当前游戏UUID及存档：平台生成正面门/回家双门替换；侧边统一短过门石，场景负责上下墙厚度和遮挡。商铺门板无独立门框，使用自然营业信息，完整源图等比；上侧铰接、48地图单位宽、按连接开向，打开仍参与碰撞。过门石亮度0.82、饱和度0.88。

## 验证

- 18项门素材、共享几何、寻路及墙体检查通过；碰撞加入后发现移开的木箱旧站位和后门相交，已将接近点改在木箱东侧并复验。
- 发布回归1130项，1129通过；唯一失败是预加载清单仍期待淘汰侧门。更新该测试以核验当前可见资源后，所属2项测试通过。
- `npm run build`（类型、空间、Vite、资源hash、Worker启动）通过；原叙事空间检查20分支/78动作通过。
- 真实RPG-JS renderer向门板持续推行650ms，停在门板边界；正常试玩照相馆进入/返回街口成功。保留390×844手机画面、桌面碰撞画面及外部访客栏检查。
- UUID、API base、源码/暂存/构建秘密扫描通过；入口和素材路径保持相对，既有远程guest-shell和存储scope保留。

## 边界

生成prompt已在平台用于两种商铺门，机械去底和自动alpha定位可复用；生成图片的语义结构仍需视觉检查，不宣称已完成无人审美审核。此次没有重新验证平台登录/跨设备存档，也没有新增或迁移后台。

发布结果在同名JSON记录；写本文时仍在发布准备阶段，不以本文件代替线上证据。
