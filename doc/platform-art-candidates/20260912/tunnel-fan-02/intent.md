# 通风机：已准入设备参考的状态图实验

旧tunnel-fan-01以早期原图为参考，供应端明确拒绝且无PNG；该请求保持终态，不恢复、不重复提交。本次采用已经透明处理并在场景中使用的starter-states-v1双状态参考，设备形态改为低矮的撬座式抽风机，以当前可用的双状态制作链完成另一份明确设计。

固定一个新请求，仅一次初始生成；取得可评审图片后最多一次针对具体问题的修正。没有图片不继续自动重发。先检查相机、完整主体、状态差别及柜体接地点，再做已授权去底/分帧/脚点对齐，原图保持。

状态合同：初始和已检查均为停转；只有tunnel-cargo-policy=retained时显示启动图。abandoned不表示风机启动。当前机器尚无旧占地，只有显式绑定新设备版本的旅程才增加它的可见占地；旧旅程碰撞和表现不改。
