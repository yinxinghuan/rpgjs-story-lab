# 两个缺失侧向接触姿态

原 hero-reproduce-01 正背向交替可辨，侧向重复，仍整体拒绝。hero-side-pairs-01 已结构化终止，不重试同请求。本次新实验只补左右各一个近腿后伸、远腿前伸、近臂前摆姿态，把四帧生成降为两帧；单次请求，不循环抽图。

先检验两侧确为不同半步、包不镜像、B 角度/头身/纹理匹配；再决定是否值得开发明确保留原图来源的图集补帧流程。未通过不得进入正式素材，不宣称整体四向行走完成。使用本项目公共媒体客户端和既有 UUID，不使用玩家照片、账号或凭据。
