# 留一盏灯 · 正交空间视觉合同

## 1. 视觉命题
雨夜停驶的列车，由玩家亲手修复与求援，一盏盏恢复可依靠的光。延续用户已认可的狭长车厢、两侧铁路、蓝布座椅、旧橄榄金属、黄铜暖光与黄雨衣。

## 2. 构图与相机
384×576 世界，正交 2D JRPG，平行边线，无消失点、无近大远小。车体约 116×528，占画面宽约 30%；两侧非行走环境表达铁路身份。人物 .14 图集尺度，主体高约 39、宽约 20，脚底碰撞 9×15。

客厢内宽 84，五排共十个 24×30 单座，排距 56、中央净通道 36。行李车内宽 96，三个沿墙 24×24 货箱，保留中央路；驾驶室内宽 84，可行动区从 y240 开始，控制台在前端凹槽，后部有 24×30 座椅。不同空间仍使用同尺度主角。

## 3. 颜色
背景 #08111b，外景 #16283e，UI #111e2c，正文 #eee4ce，次要 #b7b2a3，黄铜 #b59657，暖灯 #edc77a；连接成功用两盏青绿设备灯并辅以文字。颜色不是唯一状态提示。

## 4. 排版
中英 Georgia/系统宋体，辅助 system-ui。对白/日志 16px/1.65，小屏不缩成 12px；主行动 14px，辅助 11–13px，地点 9–11px。短分页中文 44 字、英文 110 字；长内容可滚动，菜单固定在视口内。

## 5. 形状、材质与光
深蓝布面、磨损金属、细黄铜边框；游戏家具保持直角/轻微圆角物体形状。UI 2–4px 角，44px 最小点击区域。主灯暖、窗外冷，不增加无语义霓虹特效。维修后 600ms 内加强背景/物件光感；降低动效时直接切换。

## 6. 素材与状态
三张独立背景 carriage-narrow、baggage-car、driving-cab。背景不烘焙人物或可变物件；静态座椅与货箱独立实例。原角色与物件仍用真实 RGBA 图集，逐帧/逐状态裁切与脚底锚点。

新增 train-equipment 是生图工具直接生成的全幅不透明矩形设备图集：货箱表面、调度板、电池、电台无电/有电/已连接。作为实体矩形表面消费，不伪装成透明抠图。库存与装入电台后的电池保持橄榄方形外壳/黄铜接线柱身份。图集实际分界不均匀，读取真实 crop，不能假定请求的 512 等分成立。

器材柜使用原空柜状态层，未取电池另叠电池贴图。电台无电槽空；装入后方形电池与一盏琥珀灯；求援后再亮两盏青绿灯。调度板读前/读后外形不改变，知识与日志变化由正文表达。

## 7. UI 与图标
地图是主舞台。顶部地点、标题和任务；同一套自定义线框 SVG 表示路线/背包/日志/设置，44×44 触控。路线只显示已到访地名与当前位置，未知区域显示“尚未抵达”，不提供传送。

普通器材柜开柜/取物用短提示；检修柜和电台使用同图集物件特写，NPC/调度记录使用底部对白；电台两颗具名频道按钮，可选展开自由输入。实体名称字幕不参与额外命中，NPC 名称位于左侧，避免覆盖配电箱。转场遮罩直到实际 RPG-JS 地图与玩家就绪才撤下。

## 8. 动效与声音
主角步态以实际行走距离为时钟：每 55 世界像素完成一个左右脚周期，按左迈步→并脚→右迈步→并脚循环；步速仍为 110 世界像素/秒。寻路节点之间连续移动，减速时脚步同步放慢，碰墙停止、到达或打开面板时恢复站立。帧率或手机像素密度不改变步幅。
输入同帧反馈；寻路落点标记，阻挡有短反馈；按下/焦点可见；菜单/对白焦点受控并在关闭后恢复。声音默认关，依赖手势解锁，提交成功才播短音；没有配音/循环音乐。prefers-reduced-motion 下禁用过渡动画，保留结果。

## 9. 参考转译
参考 RPG-JS 官方 starter 的正交格子、地图角色与接近互动语法，保留真实 RPG-JS renderer/Tiled/地图事件。当前直接移动适配器和 React 对话壳是为共享叙事碰撞/手机可读性所作集成，并非完全照搬原生多人输入或 showText UI。

## 10. 反模式
不把车厢扩成大厅，不为了占满画布扩大室内；不让远处物件更小；不把透明棋盘格 RGB 作为贴图准入；不把库存电池变成安装后的保险丝；不以 React 名称/热点消失冒充实际引擎转场完成；不在模型文字里创建设备/新人物；不隐藏未通过的运行问题。

## 11. 切片验收
三个区域同尺度；所有物件/门可达；十个客厢座椅、货箱、车壁与固定设备边界一致；林位于客厢、周雨位于行李车，往返后保持对应实体。完整修复→电池→记录→装入→错频道→正确频道→往返/刷新流程；390×844、320×568、1440×1000 地图及对白/库存/路线/英文菜单实际截图。素材许可证随构建分发。

## 已锁定的当前交付方向

采用 orthogonal-overhead-b-v1：B俯视图集、正交狭长车厢与两侧外景。黄雨衣主角、灰衣林、青衣配米色围巾的周雨；许岚仅通过电台出现，不创建实体人物。九项当前素材基线不再生图替换。

界面采用A信息结构、B临时提示、少量C黄铜材质用于设备特写。手机全屏铺底并等比跟随；桌面操作区单行三列。人物物理像素缓冲随地图CSS尺寸/DPR调整，最高5.2倍，不改变世界比例。落点显示正圆环。关闭特写统一写“返回地图”。

Pages设置明确说明本浏览器存档与预设对话，隐藏在线模型选项。发布保留平台guest-shell；外部访客栏不作为游戏安全区，不为它修改地图镜头。

### 发行封面

public/poster.png 是英文叙事插画海报，1024方图；标题位于顶部，黄外套旅人与暖灯作为主体。它用于发行列表与meta，不替换B方向的游戏内正交地图或人物图集。原图与160缩略图已目视检查；生成来源见doc/poster-provenance.json。

### 物件与人物的前后关系

场景独立物件和人物共用引擎的脚底深度排序；人物在物件后方时由物件遮住重叠部分，走到前方时覆盖物件。采用已准入 B 图集和原世界矩形，不重新生成素材或压缩人物高度。柜内电池随柜体排序，不能因为图像中心较高而掉到柜子后面。关闭、打开、取空、修复与电台供电状态都从同一权威存档投射。引擎色调代替原 DOM 亮度滤镜，明暗不是逐像素等同；设备特写继续使用原 SVG。


2026-09-10 接应章节延续既有 B 视角和界面。新协作/识别动作使用 cl-contact 按钮，至少 44px 高，仅在当前对白最后一页显示；人物关系为已介绍人物的文字记录。没有新角色外观、接应人员图集或外景地图，画面中不假装出现救援人员。接应章节终点是车内准备交接，照明继续由原供电事实投射。


### 终章步道（2026-09-10，开发中）

沿用 orthogonal-overhead-b-v1：新背景 public/art/rescue-walkway.png 使用现有窄车厢作为内置 image_gen 风格/尺度参考。1024×1536不透明画面；实际空地比生成要求窄，运行采用测得的x150..234、y70..528矩形而非提示词坐标。护栏及固定灯在通道外，尽头联络器复用已准入设备图集，脚点/矩形和碰撞均来自scene-layout。图中没有屋顶，称“轨旁接应步道”，不声称存在避雨棚。提示词与候选来源见rescue-art.json。旧三背景不覆盖。

客厢后端出口、步道回程口使用既有门状态图；联络器使用现有 powered/connected 两态。终章撤收临时引导后，客厢不再应急变暗/闪灯，供电选择事实仍保留为历史后果。结局复用原结果页，普通修复和移动不额外增加确认面板。真实renderer验证另记technical.md。

场景准备恢复沿用现有错误提示样式：已提交旅程的当前场景失败在地图加载遮罩中显示重试；未提交转场失败显示独立提示，可重试原行动或留在原地。避免同时显示原对白和失败对话框，焦点进入当前恢复区域；按钮至少44px。加载文案宽度限制在280px或视口减32px以内，独立于地图相机的裁切尺度。已确认的B美术、地图比例和人物尺寸不变。

可选传话支线复用林、周雨在原车厢的B图集与站立交谈；不增加假信件、搬动角色或绘制未制作物件。对白末页显示接受/婉拒或当前阶段动作。菜单“一路上的约定”使用原旅程面板、引用文案和同一套SVG记录图标；主线目标不被支线覆盖。完成后的关系记录使用人物页现有文本层级。

### 旅途画页

仅在已完成旅程的结果面板追加可选画页区域，沿用现有深蓝面板、暖金边线和正文排版；按钮至少44px，不新增导航风格。768×1024插画按原比例完整展示。插画只画无人轨旁步道，参考已认可B方向与原步道环境，不能生成新人物、门、路线或解释为新剧情事件。准备失败显示文案与恢复操作，继续探索与结果页原操作保留；不以空白图框表示素材已完成。图片须实际解码并核对尺寸、摘要后才能渲染。

画页目前仅在预发布入口测试：真实服务两次拒绝，尚无实际生成画面可评审；不以PNG元数据或模拟结果代替B视角验收，正式入口关闭。

### 原作北岬站候选（2026-09-11）

同工程预发布场景沿用B俯视角与暗石/暖灯系统：纵向停靠列车、两侧检修通道、前方空地与左侧棚屋。north-cape-v2移除了v1右上角不应烘焙的控制盒；实际碰撞以生成结果测量，不把提示词坐标当作图中事实。390×844实际renderer已检查，主角暂用已准入黄衣图集验证尺度。没有把主角或旧林图集当作原作阿达。

阿达v1虽符合候选身份描述，但PNG是RGB并画有棋盘格，透明和步态尚未准入；不展示在场景中。候选原图与完整生成记录存于original-train-candidates/20260911。当前场景检查栏仅存在预发布工具中，不是最终原作UI，正式车厢美术保持不变。


### 河谷近岸与实际取景（2026-09-11，预发布检查）

river-valley-v1 为内置 imagegen 生成的 1024×1536 不透明背景，参考北岬 v2 的 B 角度、湿石和暖灯；原图、完整提示词与摘要在同日候选目录。下半部为近岸石台，上半部为水面、断桥和远岸小建筑。远岸建筑只作环境，不声明可进入的门；人物、临时装备与设备状态仍单独准入。

检查页桌面完整显示地图；手机保留全屏铺底，人物横向和纵向移动时背景与角色层一同平移。下方检查栏占用的高度从取景区扣除，所有检查按钮和选择框至少 44px。初次入图、切换后静止与行走后都需目视对照落点，不能用世界坐标正确替代相机对齐验证。
