# 留一盏灯 · 正交空间视觉合同

## 1. 视觉命题
雨夜停驶的列车，由玩家亲手修复与求援，一盏盏恢复可依靠的光。延续用户已认可的狭长车厢、两侧铁路、蓝布座椅、旧橄榄金属、黄铜暖光与黄雨衣。

## 2. 构图与相机
384×576 世界，正交 2D JRPG，平行边线，无消失点、无近大远小。车体约 116×528，占画面宽约 30%；两侧非行走环境表达铁路身份。人物 .14 图集尺度，主体高约 39、宽约 20，脚底碰撞 9×15。

客厢内宽 84，五排共十个 24×30 单座，排距 56、中央净通道 36。行李车内宽 96，三个沿墙 24×24 货箱，保留中央路；驾驶室内宽 84，可行动区从 y240 开始，控制台在前端凹槽，后部有 24×30 座椅。不同空间仍使用同尺度主角。

## 3. 颜色
背景 #08111b，外景 #16283e，UI #111e2c，正文 #eee4ce，次要 #b7b2a3，黄铜 #b59657，暖灯 #edc77a；连接成功用两盏青绿设备灯并辅以文字。颜色不是唯一状态提示。

## 4. 排版
中英 Georgia/系统宋体，辅助 system-ui。对白/日志 16px/1.65，小屏不缩成 12px；主行动 14px，辅助 11–13px，地点 9–11px。短分页中文 44 字、英文 110 字；长内容可滚动，菜单固定在视口内。

## 5. 形状、材质与光
深蓝布面、磨损金属、细黄铜边框；游戏家具保持直角/轻微圆角物体形状。UI 2–4px 角，44px 最小点击区域。主灯暖、窗外冷，不增加无语义霓虹特效。维修后 600ms 内加强背景/物件光感；降低动效时直接切换。

## 6. 素材与状态
三张独立背景 carriage-narrow、baggage-car、driving-cab。背景不烘焙人物或可变物件；静态座椅与货箱独立实例。原角色与物件仍用真实 RGBA 图集，逐帧/逐状态裁切与脚底锚点。

新增 train-equipment 是生图工具直接生成的全幅不透明矩形设备图集：货箱表面、调度板、电池、电台无电/有电/已连接。作为实体矩形表面消费，不伪装成透明抠图。库存与装入电台后的电池保持橄榄方形外壳/黄铜接线柱身份。图集实际分界不均匀，读取真实 crop，不能假定请求的 512 等分成立。

器材柜使用原空柜状态层，未取电池另叠电池贴图。电台无电槽空；装入后方形电池与一盏琥珀灯；求援后再亮两盏青绿灯。调度板读前/读后外形不改变，知识与日志变化由正文表达。

## 7. UI 与图标
地图是主舞台。顶部地点、标题和任务；同一套自定义线框 SVG 表示路线/背包/日志/设置，44×44 触控。路线只显示已到访地名与当前位置，未知区域显示“尚未抵达”，不提供传送。

普通器材柜开柜/取物用短提示；检修柜和电台使用同图集物件特写，NPC/调度记录使用底部对白；电台两颗具名频道按钮，可选展开自由输入。实体名称字幕不参与额外命中，NPC 名称位于左侧，避免覆盖配电箱。转场遮罩直到实际 RPG-JS 地图与玩家就绪才撤下。

## 8. 动效与声音
主角步态以实际行走距离为时钟：每 55 世界像素完成一个左右脚周期，按左迈步→并脚→右迈步→并脚循环；步速仍为 110 世界像素/秒。寻路节点之间连续移动，减速时脚步同步放慢，碰墙停止、到达或打开面板时恢复站立。帧率或手机像素密度不改变步幅。
输入同帧反馈；寻路落点标记，阻挡有短反馈；按下/焦点可见；菜单/对白焦点受控并在关闭后恢复。声音默认关，依赖手势解锁，提交成功才播短音；没有配音/循环音乐。prefers-reduced-motion 下禁用过渡动画，保留结果。

## 9. 参考转译
参考 RPG-JS 官方 starter 的正交格子、地图角色与接近互动语法，保留真实 RPG-JS renderer/Tiled/地图事件。当前直接移动适配器和 React 对话壳是为共享叙事碰撞/手机可读性所作集成，并非完全照搬原生多人输入或 showText UI。

## 10. 反模式
不把车厢扩成大厅，不为了占满画布扩大室内；不让远处物件更小；不把透明棋盘格 RGB 作为贴图准入；不把库存电池变成安装后的保险丝；不以 React 名称/热点消失冒充实际引擎转场完成；不在模型文字里创建设备/新人物；不隐藏未通过的运行问题。

## 11. 切片验收
三个区域同尺度；所有物件/门可达；十个客厢座椅、货箱、车壁与固定设备边界一致；林位于客厢、周雨位于行李车，往返后保持对应实体。完整修复→电池→记录→装入→错频道→正确频道→往返/刷新流程；390×844、320×568、1440×1000 地图及对白/库存/路线/英文菜单实际截图。素材许可证随构建分发。

## 已锁定的当前交付方向

采用 orthogonal-overhead-b-v1：B俯视图集、正交狭长车厢与两侧外景。黄雨衣主角、灰衣林、青衣配米色围巾的周雨；许岚仅通过电台出现，不创建实体人物。九项当前素材基线不再生图替换。

界面采用A信息结构、B临时提示、少量C黄铜材质用于设备特写。手机全屏铺底并等比跟随；桌面操作区单行三列。人物物理像素缓冲随地图CSS尺寸/DPR调整，最高5.2倍，不改变世界比例。落点显示正圆环。关闭特写统一写“返回地图”。

Pages设置明确说明本浏览器存档与预设对话，隐藏在线模型选项。发布保留平台guest-shell；外部访客栏不作为游戏安全区，不为它修改地图镜头。

### 发行封面

public/poster.png 是英文叙事插画海报，1024方图；标题位于顶部，黄外套旅人与暖灯作为主体。它用于发行列表与meta，不替换B方向的游戏内正交地图或人物图集。原图与160缩略图已目视检查；生成来源见doc/poster-provenance.json。

### 物件与人物的前后关系

场景独立物件和人物共用引擎的脚底深度排序；人物在物件后方时由物件遮住重叠部分，走到前方时覆盖物件。采用已准入 B 图集和原世界矩形，不重新生成素材或压缩人物高度。柜内电池随柜体排序，不能因为图像中心较高而掉到柜子后面。关闭、打开、取空、修复与电台供电状态都从同一权威存档投射。引擎色调代替原 DOM 亮度滤镜，明暗不是逐像素等同；设备特写继续使用原 SVG。


2026-09-10 接应章节延续既有 B 视角和界面。新协作/识别动作使用 cl-contact 按钮，至少 44px 高，仅在当前对白最后一页显示；人物关系为已介绍人物的文字记录。没有新角色外观、接应人员图集或外景地图，画面中不假装出现救援人员。接应章节终点是车内准备交接，照明继续由原供电事实投射。


### 终章步道（2026-09-10，开发中）

沿用 orthogonal-overhead-b-v1：新背景 public/art/rescue-walkway.png 使用现有窄车厢作为内置 image_gen 风格/尺度参考。1024×1536不透明画面；实际空地比生成要求窄，运行采用测得的x150..234、y70..528矩形而非提示词坐标。护栏及固定灯在通道外，尽头联络器复用已准入设备图集，脚点/矩形和碰撞均来自scene-layout。图中没有屋顶，称“轨旁接应步道”，不声称存在避雨棚。提示词与候选来源见rescue-art.json。旧三背景不覆盖。

客厢后端出口、步道回程口使用既有门状态图；联络器使用现有 powered/connected 两态。终章撤收临时引导后，客厢不再应急变暗/闪灯，供电选择事实仍保留为历史后果。结局复用原结果页，普通修复和移动不额外增加确认面板。真实renderer验证另记technical.md。

场景准备恢复沿用现有错误提示样式：已提交旅程的当前场景失败在地图加载遮罩中显示重试；未提交转场失败显示独立提示，可重试原行动或留在原地。避免同时显示原对白和失败对话框，焦点进入当前恢复区域；按钮至少44px。加载文案宽度限制在280px或视口减32px以内，独立于地图相机的裁切尺度。已确认的B美术、地图比例和人物尺寸不变。

可选传话支线复用林、周雨在原车厢的B图集与站立交谈；不增加假信件、搬动角色或绘制未制作物件。对白末页显示接受/婉拒或当前阶段动作。菜单“一路上的约定”使用原旅程面板、引用文案和同一套SVG记录图标；主线目标不被支线覆盖。完成后的关系记录使用人物页现有文本层级。

### 旅途画页

仅在已完成旅程的结果面板追加可选画页区域，沿用现有深蓝面板、暖金边线和正文排版；按钮至少44px，不新增导航风格。768×1024插画按原比例完整展示。插画只画无人轨旁步道，参考已认可B方向与原步道环境，不能生成新人物、门、路线或解释为新剧情事件。准备失败显示文案与恢复操作，继续探索与结果页原操作保留；不以空白图框表示素材已完成。图片须实际解码并核对尺寸、摘要后才能渲染。

画页目前仅在预发布入口测试：真实服务两次拒绝，尚无实际生成画面可评审；不以PNG元数据或模拟结果代替B视角验收，正式入口关闭。

### 原作北岬站候选（2026-09-11）

同工程预发布场景沿用B俯视角与暗石/暖灯系统：纵向停靠列车、两侧检修通道、前方空地与左侧棚屋。north-cape-v2移除了v1右上角不应烘焙的控制盒；实际碰撞以生成结果测量，不把提示词坐标当作图中事实。390×844实际renderer已检查，主角暂用已准入黄衣图集验证尺度。没有把主角或旧林图集当作原作阿达。

阿达v1虽符合候选身份描述，但PNG是RGB并画有棋盘格，透明和步态尚未准入；不展示在场景中。候选原图与完整生成记录存于original-train-candidates/20260911。当前场景检查栏仅存在预发布工具中，不是最终原作UI，正式车厢美术保持不变。


### 河谷近岸与实际取景（2026-09-11，预发布检查）

river-valley-v1 为内置 imagegen 生成的 1024×1536 不透明背景，参考北岬 v2 的 B 角度、湿石和暖灯；原图、完整提示词与摘要在同日候选目录。下半部为近岸石台，上半部为水面、断桥和远岸小建筑。远岸建筑只作环境，不声明可进入的门；人物、临时装备与设备状态仍单独准入。

检查页桌面完整显示地图；手机保留全屏铺底，人物横向和纵向移动时背景与角色层一同平移。下方检查栏占用的高度从取景区扣除，所有检查按钮和选择框至少 44px。初次入图、切换后静止与行走后都需目视对照落点，不能用世界坐标正确替代相机对齐验证。
# 平台素材候选验证补充（2026-09-11）

浏览器制作入口复用深蓝背景、金色重点和浅色文字；标题 24px、正文和按钮 16px、间距 8/16/24px，触控高度至少 44px。单列流程先选择光照并生成，随后展示候选及“在地图中试走”。等待、失败、继续任务与待质量验收均明确显示文字；小屏内容纵向滚动，不把制作面板永久放入玩家 HUD。

已锁定 B 视角和既有清晰度继续作为目标。公共媒体服务的单参考北岬背景在同地图中保持了检修点及人物落点关系，但地面更暗、纹理更平滑，未接受为质量持平。当前原始人物/设备输出均为 RGB 假透明，不进入实际角色层。候选对照仅用于 preflight；背景切换不改变地图几何和既有人物。详见 `platform-art-production-plan.md` 与 `platform-art-candidates/20260911/runtime-review.json`。

素材准备页沿用已部署制作页的深蓝底、暖白正文和金色主按钮，16px正文、48px输入、44px以上点击目标。主要动作是生成透明候选；参数在源图之后，候选比较在操作之后，记录放折叠区。原图/处理图在同一预览区切换，可选浅色、深色检查底；透明检查底不被导出到PNG。320与390宽纵向滚动，不依赖hover，不做新风格选择。该界面是制作工具，不影响已选游戏HUD结构。

人物候选试走只在原作场景检查页出现，页首标明“未准入正式游戏”，底部提供明确的基准回退和返回准备页。沿用原地图视口/碰撞/距离步态，新增对照行的实际高度参与手机相机可见区计算。以已认可B主角的alpha>200可见高度中位数236源像素×原缩放0.14=33.04世界像素作为整张候选图集的统一高度目标；不逐帧拉伸。低分辨率彩色矩形仅属合成方向/帧诊断，不能据此评审真实角色质量。

### 原作会话界面开发态（2026-09-11）

延续已选A信息结构与B临时提示，原B主角尺度/四向步态不变。地图铺底、顶部紧凑资源与地点、底部目标和累计记录入口；物件局部动作面板可滚动，44px触控目标，正文16px且中英换行。加载/恢复时暂停人物，保留旧画面并展示重试；面板关闭原地继续。开发入口标明“原作流程开发版·美术未完成”，未准入物件为简单编号/名称的SVG标记，不冒用现有其他NPC造型。北岬/河谷沿用只读背景候选，其余地图用共享碰撞几何的灰色占位图。当前只作本机流程验证，不申请正式视觉通过；生产依旧需要替换完整准入资产并同状态复验。


原作北岬背景的固定平台版本8fc11a96进入新建本机旅程：保留B俯视、雨夜铁路与既有碰撞布局，采用该已评审版本的冷光/暖灯细节。旧旅程保留e8e36bba基准，不随默认新素材改外观。人物与未完成设备仍明确开发占位；单张背景准入不改变其余类别的门槛。390/320真实构图、左侧维修/右侧制动可达性、车体阻挡和刷新版本已复验。

原作故障恢复以单个对话框显示，焦点位于44px恢复按钮；原行动面板在故障期间隐藏，避免两个模态面板竞争。场景加载失败与请求未确认分别说明，恢复不改变既有美术方向。320×568、390×844平台构图已复拍，Tab/Shift+Tab/Return恢复通过。

原作已在场人物面板增加交谈/行动两种输入意图，沿用原有局部面板、字号、44px按钮与金色选中态，不扩展新的画面风格。人物默认交谈，物件只输入行动；既有作者行动按钮始终直接执行其已登记动作。交谈记录使用明确“你/人物名”的说话者标记，累计正文不复制出摘要或资源清单。默认本机模式明确提示可询问目标、表达担忧或回忆最近原话，不伪装成已开放任意模型对白。

制作页设备检查沿用B视角地图和原有检查面板：三帧共用同一柜体接地点与整图缩放，关柜主体高50世界单位，开门占位预先保留。状态按钮显示选中态，柜前/柜后/碰撞检查使用实际引擎路径与深度排序。候选标题始终说明未准入正式游戏；不把合成色块的通过记录作为平台真实设备的画质验收。

原作人物交谈面板在可用预演中提供同一按钮体系的在线交谈开关和简短数据发送说明，44px 触控目标。明确拒绝保留在当前面板并保留输入，不跳到旧日志遮住原因。390×844 与320×568实际检查；只补交谈必要状态，整体 UI 精修仍后置。

原作物件/线路面板可在预演预算可用时开启在线行动理解，沿用交谈开关的同一按钮体系。说明明确只发送输入与当前可用行动，场景准备好再执行；失败沿用单一恢复弹层。390尺寸实际线路输入与320失败恢复/抵达画面已检查，B视角保持。

在线草稿沿用制作页的深蓝背景、暖金按钮、16px正文与48px控件。候选图后的在线保存是次级动作；在线列表按保存时间排列，用编号、光照与日期辨认，行按钮用click允许纵向滚动。等待即时可见、失败紧邻恢复动作，320/390不依赖hover。明确区分浏览器草稿、在线保存和未开放的正式发布。

地图检查面板显示已完成的5项检查数量，并提供“确认画面并完成检查”。仅背景候选且使用基准人物/设备时开放；完成后的制作页显示发布动作及“以此背景开始新旅程”，说明背景会公开、新旅程不会覆盖旧旅程。沿用当前控件体系，不增加新的独立演示页面。


B黄色主角背向步态修复：保留原`hero.png`，运行默认改用`hero-gait-v2.png`。只在背向第三帧、帧内y266以下对腿部进行水平反向，上半身/背包、另11帧及源图像素不变，画布1086×1448、脚底330与缩放0.14保持。该阈值来自本张图的逐像素检查，不适用于其他角色。基准图原来两迈步帧同腿在前，今后角色准入必须逐方向核验两条腿的先后确实反转，背包/灯具不随帧跳边，不能以位移同步测试代替动作语义检查。

阿达正面站姿接入评审：平台actor-edit-03原图经现有制作页同一去底/对齐算法处理，单独裁出第0行第1列320×320，不修改姿态。脚点160,300、整图缩放0.16，实际主体约34世界单位高，与B主角相近。场景中使用真实引擎事件和地面深度排序，点击区透明、名字在脚下；其他未准入人物保留开发标记。整张图集的方向错误仍拒绝，不能因一帧可用宣称人物动画生成已通过。

启动继电器使用平台starter-edit-02的第1列铜线圈，与starter-repair-03单帧局部修复结果组成两个独立状态。明确排除02中错误变成陶瓷保险丝的第2列。两帧统一缩放40/326，脚点160/544，以原柜体接地点173/463、173/468平移对齐，不逐帧拉伸。原图保留；新图真实alpha，开门最大范围统一预留。修复帧柜体高度约增加4.6%（世界高度约1.84单位），这是当前候选的残余形变，不宣称像素完全一致或工程结构图准确。沿用B角度与实际引擎脚底排序；完整手机视觉及平台最终验收仍待完成。

创作者设备准备增加折叠的修复前/后组合区：两组PNG文件、总列数与选用列，48px控件，320px下fieldset允许收缩、文件名换行。没有重新选择视觉风格。两帧与旧三帧状态采用各自标签，不能把修复后显示成“开柜有物”。地图检查统一预留最远开门轮廓，包括柜体落点前方伸出的门角；前方接近点随此范围调整。

### 在线人物与设备素材区（2026-09-11）

沿用制作页深蓝背景、浅金文字与描边、纵向全宽48px按钮。在本地素材记录下显示在线保存/查看入口，每条已完成记录提供取回；只有未完成上传出现清除按钮。长文件名允许换行，进度和失败以就近文字说明，不使用新的图标或弹窗。保存与发布状态明确分开：归档成功不显示“已准入”，取回后继续原有浅/深底预览和地图状态检查。320×568与390×844使用同一纵向滚动结构，真实截图记录在本轮归档评审中。

修复设备的检查区增加折叠的5项检查：修复前、修复后、柜前、柜后、柜体阻挡。实际到位/显示后才记为已执行，最后另有显式画面确认；该记录属于创作者观察声明。检查页的启动装置使用原作110/160落点和40世界单位主体高度，原三状态柜体仍用旧检查位与50高度。发布按钮只有该候选检查齐全时开放；在线列表中的已发布设备提供独立旅程入口。重新处理生成的新候选必须重新检查，不能继承旧版本的确认。


在线人物/设备原图入口沿用制作页暗蓝底、黄铜主按钮、16px正文与48px控件。一个配方选择与一个生成/续接主动作；任务状态、失败原因和可导入原图紧随其后。生成成功只标原图保存，人物必须逐方向检查左右腿交替/携带物侧别/脚点。设备单帧先在原有组合区从已保存记录选择两张图，不自动拉伸或发布。手机保留页面纵向滚动，无新浮层。

原作恢复弹层区分两种操作：普通网络故障“重新连接并恢复”；地图加载超时“重新载入地图并恢复”。后者解释会恢复同一旅程和已保存操作；沿用现有弹层/焦点管理/按钮尺寸，无自动刷新循环。

创作者正式版本发布后提供“查看已发布的背景图/设备图”；只有已启用原作的preflight显示进入绑定旅程。布局与样式沿用现有制作面板。

设备组合原图选择器同时列出独立来源和旧组合保留的两张原图，后者注明原来的修复前/后位置与选用列。相同来源/列/生成记录的处理副本合并展示；沿用原生select与当前44px以上触控控件，不改变整体布局。

人物检查面板沿用制作页深蓝底、暖色字和现有48px控件。折叠区内选择一个方向，等比并排显示左迈步/站立/右迈步三个原始帧位置（标签使用“第1帧/站立/第3帧”，不预先断言腿侧正确）；可切浅深底，逐项选择未检查/通过/不通过。不用绿色总状态暗示正式准入；结论使用明确文本“待检查/发现问题/图集检查已记录，仍需地图检查”。图片只在页面中裁切展示，不生成或改写素材。

人物检查在线保存沿用原有在线素材按钮，不增加独立页面。局部保存按钮仍明确只保存本地；说明在线保存会追加检查版本、取回恢复最新记录、每份最多64版。文件已在线保存但检查回执丢失时，就近显示可重试说明，不能显示整个操作成功。取回后仍展示未检查/有问题状态，保留地图验收要求。320×568中文和390×844英文实际操作与截图确认48px控件、长句换行和纵向滚动，无横向溢出。
人物候选试走在现有地图页增加折叠的8项地图检查，沿用深蓝底、金色边框和44px以上按钮。小屏检查区最多45dvh并内部纵向滚动，为真实地图保留上方空间；普通背景与设备检查页不改。未通过12项图集检查时就近显示原因并禁用“确认人物画面并保存检查”。机器执行完成只显示试走步骤完成，画面确认仍是显式操作。制作页恢复后显示该版本含地图确认，修改图集检查会清空确认，不用绿色或“已发布”暗示素材准入。

人物发布沿用在线素材区域的单列布局。按钮明确写“公开发布阿达人物版本”，相邻说明要求核对机修学徒及机务灯身份、图集和地图检查；已发布记录显示角色和固定版本查看入口。测试版本可进入独立原作旅程，正式版本只查看图集。阿达朝玩家方向使用四张站姿，沿用B俯视角、统一脚点与地面深度；不通过镜像整个身体制造左右帧，避免灯具换手。原有背向重复迈同腿的阿达整图仍拒绝，不能因发布链跑通而成为合格素材。

灰石货场背景（2026-09-12）：沿B正交俯视、固定柴油车头与左右检修道，使用浅灰碎石/混凝土、石墙、锈蚀铁皮棚和小面积暖灯，区别于北岬的蓝黑夜色。首张右上巨石侵入通路，定向修正后清除；棚屋屋檐剩余外伸由本图专属碰撞覆盖，上下缘预留分别14×14和14×4世界单位。图片以1024×1536原比例投射384×576，保持可读性，不烘焙新人物/油泵状态。320/390实际截图核对停步位置、列车/人物比例和小屏构图。玛柯、油泵等尚以开发标记呈现，当前只准入场景背景及其碰撞，不宣称该章全部美术完成。

### 2026-09-12 三地环境准入规则

黑松林线、白石隧道与沉睡小城延续已锁定的 B 俯视方向和窄列车尺度，均通过平台媒体服务制作；PNG 原样保留。林线第二张仍有局部树冠伸入坐标提示的通道，小城和隧道的左侧小屋亦有屋檐偏差，使用逐图量取的保守不可行走轮廓解决，不能把提示词坐标视为精确几何保证。地面明暗、树冠、墙面为场景背景；人物、风机、广播设备、烟雾状态仍需独立准入，背景验收不涵盖这些开发标记。

所有额外边界只属于对应背景摘要及 TMX 版本。生成 → 查原图 → 记录偏差 → 配碰撞 → 验所有互动点可达 → 实际引擎行走/转场 → 固定到新旅程，是本轮可重复的环境制作流程。未完成最后两项前不将候选标记为通过。旧旅程不追随新素材默认值。

### 2026-09-12 后三地：场景身份与可变剧情分离

山口采用坡前检修台和崖缘，洪桥采用近岸混凝土检修平台与水域，岔站采用独立砖砌调度院落。岔站没有画入列车：保车分支的列车停在院落画外，留桥分支的列车仍固定在桥上。不能复用“到处都有列车”的背景来抹掉牺牲结果。

洪桥第二图移除了小屋，左侧保留一处旧屋阴影；以完整版本布局移除旧小屋障碍，阴影仍可行走，仅护栏与平台外水域阻挡，不能在空地保留隐形墙。岔站第二图清除了穿屋铁轨，但建筑仍宽/长于提示，保留实际建筑包络并将本图任医生、林澈站位南移30世界单位。UI、物理占地、可接近位置和实际引擎素材槽共用版本布局。未绑定新图的旧旅程不移动角色、不改变地面。

2026-09-12 制作页步态检查：沿用深蓝/暖色表单，三帧并列下增加最多200px的单帧查看区，手机按钮两列、站立按钮整行，最小48px。默认静止，用户主动播放；不加入游戏HUD或改变已锁定B视角。

2026-09-12 保存旅程面板沿用现有记录页与底部sheet，不扩展常驻HUD。进入记录后选择“查看与继续旧旅程”，列表为可滚动大按钮，当前旅程以文字标注并禁用；新开局按钮明确保留旧旅程。加载与错误在当前面板表达，320/390中英文检查。
